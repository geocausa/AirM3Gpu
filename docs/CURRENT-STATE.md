# Current G15 Bring-up State

Research state: 2026-08-30

Last live checkpoint: 2026-08-27

## Hardware identity

Target: J615 MacBook Air M3 / T8122.

Runtime identity registers:

- `ID_VERSION = 0x07022000`
- `ID_COUNTS_1 = 0x0011010a`
- `ID_COUNTS_2 = 0x00040404`
- active core mask: `0x3ff`
- topology: 1 MGPU/cluster, 10 cores, 10 fragment units, 4 GPs

## Closed runtime gates

The following stages have been independently exercised on real hardware with fail-closed recovery:

1. Device discovery and G15G C0 identity validation.
2. GFX ASC start/stop and 42-bit G15 UAT handoff.
3. Exact J615 14-state `PwrConfig` validation.
4. Byte-validated G15 startup InitData/HwData images.
5. RTKit management protocol v12, EP1 crashlog backing, and EP20/EP21 application endpoint startup.
6. Native G15 `MSG_INIT` handoff and post-init bootstrap.
7. Persistent GpuManager/RTKit lifetime and DRM registration.
8. `/dev/dri/renderD128` exposure with submission paths still explicitly lab-gated.
9. Safe GET_PARAMS, VM creation/destruction, GEM host lifecycle, and unbound VM mappings.
10. Passive queue/context lifecycle with host-only fail-closed cleanup.
11. Native q22 mapping notification/pressure handling.
12. G15 shared-bank1 and range-7 page-table spine/L3/leaf handling.
13. J615 MTR sensor initialization sufficient to remove the earlier MTR alarm boundary.
14. A signed, one-shot Compute QueueInfo registration using one already-satisfied firmware Barrier record, followed by clean native G15 `ReleaseResource` teardown.

No normal render workload is enabled at this checkpoint.

## Historical pipe transport boundary

The original bounded empty QueueInfo probe exposed no command-ring entry. For Compute priority 2 it advanced the G15 TX `WriteIndex` from 0 to 1 and rang EP21, but firmware initially left Read/CFI at zero. That historical boundary drove E041-E056.

The exact G15 TX descriptor remains:

1. ReadIndex
2. CFIIndex
3. WriteIndex
4. ring GPU pointer

The Linux PipeChannel descriptor and RuntimePointers-exported descriptor reference the same objects, and the Compute/priority-2 work doorbell is `0x008300000000000a`.

This boundary is now superseded. E057-E060 reach scheduler acceptance and retire the barrier-only registration to `(Read, CFI, Write, Shadow) = (1,1,1,1)`, then complete native context/resource release. No RunVertex/RunFragment/RunCompute command is used by that proof.

## Submission-time wake/power closure

Apple G15 has a priority wake note separate from the work doorbell. For priority 2 the exact note is `0x0083000000000008`. Sending that note before the bounded Compute publication does not advance Read/CFI.

`AGXAccelerator::notifyFirmware(priority, false)` also calls `ensurePoweredHardware(false)`, but exact J615 reconstruction proves that this function performs no submission-time power transition on the target:

- `isPowerManagedInAGX()` is `+0x650` bit 10 and J615 leaves it clear;
- base `configureDevice()` writes `strh 5` at accelerator `+0x6c0`;
- little-endian layout therefore initializes feature byte `+0x6c1 = 0`;
- the associated `+0x5d1` static-power gate is consequently false;
- `ensurePoweredHardware(false)` bypasses `changePowerStateTo(1)` and returns.

The missing boundary is therefore not a normal submission-time PMGR or GFX wake operation.

## Ruled out at this checkpoint

Current evidence rules out:

- incorrect G15 TX qword ordering;
- CFI being a host WriteIndex shadow;
- a stale RuntimePointers pipe descriptor;
- an obvious missing CPU/DMA ordering barrier before EP21;
- wrong Compute/priority-2 work-doorbell encoding;
- missing G15 priority wake note;
- missing normal J615 `ensurePoweredHardware(false)` transition;
- the earlier MTR sensor initialization failure;
- absence of the shared-bank1/range-7 mapping backend.

## Callback-gate closure — E041 through E043

E041 passively decoded statistics tag `0x0f`: the record contains a software-state transition and reports `state=1` during the failed empty publication. The pipe nevertheless remains `Read=0, CFI=0, Write=1`, so the tag is not a retirement acknowledgement.

E042 then reconstructed the real G15 firmware callback boundary. `g15_pipe_work_callback` checks an internal runtime-power byte at `DAT_fffffc000010e528` with mask `0x78` before calling scheduler `FUN_fffffc0000006a0c`. Compute maps to callback argument 2. Its narrow bypass calls `FUN_fffffc000000cf58()`, which returns nonzero only when one of sixteen firmware work-state slots is already non-idle.

The callback can emit KTrace `0x100` on entry and `0x207` when the internal gate blocks scheduler entry. Both are guarded by trace-class bit 2 copied from q21 `host_flags`, and the records are written into the exact KTrace ring Linux already publishes.

E043 enabled only that trace class after all existing pre-RTKit exactness checks and before `MSG_INIT`. The boot remained healthy and the signed empty Compute publication reproduced the same timeout with no crash or IOMMU fault. Selected `0x100/0x207` records were not observed, but that result is not yet sufficient to claim the callback was skipped because the KTrace class itself had not been independently calibrated live.

## E045 closure

E045 enabled q21 trace bits 1+2 (`host_flags=0x6`) and repeated one signed empty Compute/priority-2 publication. Firmware emitted one callback-entry `0x100` record (`args[0]=1`), no callback-blocked `0x207`, and a healthy class-2 calibration stream. The TX pipe still remained `Read=0, CFI=0, Write=1` and timed out fail-closed.

The callback argument is a dispatch/wake class, not PipeType: `FUN_6a0c` subsequently scans all four priorities and all V/F/C channels. Thus arg 1 does not imply a Fragment doorbell.

The outer RTBuddy runtime-power gate is exonerated for this failure because callback entry is observed and the `0x207` blocked path is not taken.

## E046/E047 scheduler-entry control

E046 exposed the existing bit-1 scheduler markers (`0x111`, `0x112`, `0x128`) but retained trace bit 2, whose high-rate `0x213` stream could pressure the 512-entry KTrace ring. E047 therefore repeated the same experiment with **bit 1 only** (`host_flags=0x2`). This removed the class-2 pressure confounder without changing queue, power, PMGR, doorbell, MMU, or submission semantics.

The E047 signed empty Compute/priority-2 publication produced exactly one callback-entry `0x100` record (`args[0]=1`) and then no callback exit `0x101`, no scheduler snapshot `0x112`, no accepted-entry `0x111`, and no bounded scan `0x128`. These counts remained unchanged more than a minute later. The TX pipe remained `Read=0, CFI=0, Write=1` and timed out fail-closed.

Exact callback control flow is: entry trace -> runtime-power gate -> `FUN_fffffc000003d330()` -> `FUN_fffffc0000006a0c(param_1)` -> exit trace. Because entry is observed but neither the first `FUN_6a0c` trace nor callback exit ever appears, the active firmware thread does not return from **`FUN_3d330()`**, the cold power/setup transaction.

The callback argument is a dispatch/wake class, not PipeType; `FUN_6a0c` itself scans all priorities and V/F/C rings. The earlier assumption that Compute must imply callback arg 2 is therefore rejected.

The next experiment should remain passive: correlate q21/q4 host-visible shared power/runtime fields with the `FUN_3d330` transaction at EP20 receive times. No direct PMGR register poke and no real command-buffer submission is justified yet.

See `research/g15/G15-PIPE-SUBMISSION-BOUNDARY.md` and `research/g15/G15-PIPE-CALLBACK-GATE.md`.

## E048-E051 cold-power handshake closure

E048 added passive q21 snapshots around the bounded publication. During the failed first Compute publication q21 changes from `busy=0, unk10=0, ready=1, power=0` to `busy=0, unk10=1, ready=1, power=1`. The scheduler `busy` bit never asserts.

E049 attempted broad class-2 power-dispatch logging but amplified a hot firmware trace stream into millions of host log records; that experiment is retained only as an instrumentation failure. E050 corrected the receiver by draining KTrace without generic per-record logging and promoting only bounded decision IDs. In the clean run, callback `0x100` and pstate `0x200` appeared; the state-1 KTrace timestamp matched statistics tag `0x0f/state=1`, while no callback exit or scheduler marker appeared.

E051 then admitted `FUN_447ec` arg `1` into the bounded `0x20b/0x20c` filter. The live sequence was:

- callback entry `0x100`, arg 1;
- power-dispatch entry `0x20b`, arg 1;
- pstate `0x200`, state 1;
- power-dispatch exit `0x20c`, arg 1;
- q21 settles at `busy=0, unk10=1, ready=1, power=1`;
- TX Read/CFI remain zero and the bounded probe times out fail-closed.

Exact firmware ordering matters: `FUN_447ec(case 1)` calls `FUN_14f98(..., state=1)`, then executes `FUN_4a560(&event_slot_1)`, and only afterward emits `0x20c`. Therefore E051 proves both the state-1 power worker and the event-1 post primitive return successfully. `FUN_18864` and `FUN_d300` in the later `FUN_3d330` tail are non-blocking bookkeeping. The current target is the remaining short post-handshake tail before `FUN_6a0c`, beginning with the always-fired `0x10078` notification path and the q22 `+0x4030` optional branch.

## E052-E060 — scheduler registration lifecycle closure

E052 confirms q22 `+0x4030` remains zero across the old timeout, ruling out the optional CPMS tail as the blocker. E056 then identifies the material host-layout error: G15 HwDataA `+0x4188..+0x41db` is a DPE leakage-update image, not the sparse SoCHot structure previously assigned there. Apple's exact J615/C0 image includes HwDataA `+0x41d8 = 1` and mirrored q23 `+0x1a8 = 1`. Firmware case `0x14` consumes this state when arming its DPE timer.

After restoring the DPE image, E057 crosses the long-standing pre-scheduler boundary:

- callback `0x100` is observed;
- q21 `busy` changes `0 -> 1`;
- scheduler `0x112` snapshots appear;
- one `0x111` record accepts the Compute/2 QueueInfo.

E057 then crashes because the old diagnostic exposed untouched stamp-index sentinel `0x80`. E058 proves that merely substituting EventManager slot 0 is insufficient: firmware's internal per-slot state pointer is still null. Static reconstruction identifies the missing edge in the G15 Barrier/type-4 helper, which binds the RunWorkQueue stamp slot to QueueInfo `+0x18` (`gpu_buf`).

E059 publishes exactly one already-satisfied firmware Barrier record at QueueInfo ring entry 0 and uses `wptr=1`. There is still no RunVertex, RunFragment, or RunCompute object and no shader command stream. The result is the first complete registration:

- `0x111` accepts the QueueInfo;
- callback exit `0x101` appears;
- the pipe reaches `(Read, CFI, Write, Shadow) = (1,1,1,1)`;
- QueueInfo registration returns success;
- context/resource bytes become `(02,00,00,0f)`.

E060 closes teardown. Apple's G15 `submitReleaseResource()` opcode is `0x11`; Linux's versioned enum accidentally encoded `0x12` because it retained a legacy V13.3 `Unk0d` placeholder on G15. Excluding that placeholder from G15 numbering, plus a fail-closed pre-send `0x11` assertion, makes native `ReleaseResource` return success. The temporary command-submission gate returns `1 -> 0`, software-state telemetry returns `1 -> 0`, and no RTKit/GPU/DART/kernel fault is observed.

The current boundary is therefore **after scheduler registration and native resource retirement**. The next work is static reconstruction of the smallest real G15 scheduler command contract. Live shader/render/compute execution remains blocked until its pointer, stamp, dependency, and completion prerequisites are closed.

See `research/g15/G15-QUEUE-REGISTRATION-LIFECYCLE.md`.

## E061-E063 — first Compute execution contract

E061 statically closes the next scheduler boundary: a normal G15 RunCompute/type-3 command is inherently hardware-facing. RTKit installs command `+0x760` as the engine-2 execution stream, and Apple unconditionally produces the G15 Compute RegisterArray plus SKU stream. There is no mechanically justified parser-only RunCompute shortcut.

E062 closes the SKU framing ambiguity. The ordinary no-feature stream is `0x2b8` bytes before alignment and exactly `0x2c0` after Apple's 0x40-byte rounding. Its fixed skeleton is opcode `0xb` + 0x1b8 payload, a 0x3c-byte start timestamp record, one Compute WFI dword `1`, a 0x3c-byte end timestamp record, a 0x7c-byte opcode-`0xc` record, and finish dword `0x40000002`. The optional paired feature records make the aligned stream `0x300`.

The two remaining G15G dynamic register IDs are also fixed for J615: accelerator vslots `+0x10a8/+0x1090` return `0x101d8/0x107a0`. Register `0x1a420` still receives the raw Compute control-stream pointer, so reproducing SKU framing alone does not make a live command inert. The current target is a mechanically proven harmless CDM control stream plus its UMA, stamp, timestamp, and completion prerequisites. No live RunCompute is enabled.

E063 closes the next host-side layer. `generateRegisterList()` directly programs CDM register `0x1a420` from the raw Compute control-stream pointer. Apple's Gen4 `patchCDMControlStreamAndReset()` emits exact `0x60000160` and `0x60000960` token forms through separate CDM token pools, with 16-byte pointer/state patch records carrying the `0x20000000` address encoding, then clears cached stream state. `endComputePass()` invokes this framing before its normal `0x40000000` end-of-pass command. These are exact patch/reset records, not yet proven harmless standalone payloads. The current target is therefore semantic classification of the smallest no-threadgrid/no-shader CDM token sequence before any live RunCompute.

E064 cross-checks those constants against Mesa's independent CDM grammar: `0x60000160/0x60000960` are Barrier blocks, `0x20000000` is Stream Link, and `0x40000000` is Stream Terminate. Apple G15 emits the terminate token as one dword. A terminate-only root therefore contains no Launch block by construction; the remaining proof is that a normal Apple zero-dispatch Compute container can carry that root with valid associated objects. No live RunCompute is enabled.

See `research/g15/G15-COMPUTE-LAUNCH-BOUNDARY.md`, `research/g15/G15-COMPUTE-SKU-STREAM.md`, and `research/g15/G15-COMPUTE-CONTROL-STREAM.md`.

E065 then captures Apple’s normal direct Compute CDM packet: one 1x1x1 launch is 10 dwords (`Launch Word0`, pipeline/Launch Word1 state, global size, local/workgroup size) followed by the ordinary `0x60000160` Barrier. This independently identifies the first 40 bytes as the actual dispatch block rather than patch/reset framing.

E066 moves the outer-container proof onto the exact macOS 14.8.3 / 23J220 ABI used by the M3/J615 Linux target. Apple’s own `AGXFirmware::configurePoolElementSizes()` fixes Compute/CLE at **0x880 bytes**, exactly matching the independently reconstructed Linux `RunComputeG15V14_7`. The exact 23J220 accelerator-ring entry is **0x18 bytes** with fields at `+0x00/+0x08/+0x10/+0x14/+0x16/+0x17`, also exactly matching Linux; Compute is data-master/pipe type **2**. Exact 23J220 `submitReleaseResource()` again uses opcode **0x11**.

E067 closes the cross-build RegisterArray identity question on the exact target. The matching 23J220 KDK exposes kernel-side `AGXCLChannelG15::generateRegisterList()`, and its exact order, descriptor offsets, J615 dynamic IDs, `0x1a440`/`0x1a458` synthesis and optional tail match the independently reconstructed 25F84 program.

E068 then captures Apple’s stock no-dispatch Compute container and closes the remaining ordinary-list values. Its exact 0x1d0 raw Compute payload has all RegisterArray-fed raw state zero. Host-driver reconstruction further proves descriptor `+0x460/+0x468/+0x470` come from raw Compute `+0x158/+0x15c/+0x160`; these are also zero in the empty oracle. The resulting J615/G15G list is exactly 20 entries / `0xf0` bytes, with `0x1a440=0x154024201`, `0x1a458=0x10c08860`, and `0x107a0=0x00ff0000`. Linux now models this exact **empty-path** list compile-only. Ordinary G15 submission remains rejected with `ENODEV`, and no live Linux RunCompute is enabled.

E069 then proves on the exact 23J220 KDK that this empty descriptor is **not host-elided**. `processCompute()` proceeds through `processComputeSetup()`, `addComputeToWorkqueue()`, `AGXCLWorkQueue::submitCommand()`, and the ordinary CL-channel vslot `+0x148`; the exact G15 vtable resolves that slot to `AGXCLChannelSKU::submitBuffer()`. The same exact-target image also confirms the E062 SKU grammar (`0xb + 0x1b8`, 0x3c timestamps, WFI dword `1`, trailing `0xc`, final `0x40000002`; sizes 0x2c0/0x300). Compute data-master 2 selects `AGXAccelerator::submitCLChannel()`. Apple therefore carries the stock `0x1a420=0` no-dispatch descriptor through normal G15 Compute command construction/submission rather than dropping it on the host. Linux still does not issue RunCompute; UMA/context/timestamp/stamp/completion/recovery prerequisites remain the live boundary.

E070 closes the SKU source map on that same 23J220 image. `submitBuffer()` calls G15 register generation first and SKU encoding second, passing descriptor `+0x350` as the 0x880 command GPU/FW address. Compute timestamp packets use command-relative `+0x810/+0x818/+0x820/+0x828/+0x868`. Accelerator `+0x2488` is `AGXPerfCtrSamplerGen15`, proving the optional `0x90000004/0x10000004` pair is performance-counter state. Crucially, Linux must not use the inherited StartCompute/WaitForIdle/FinalizeCompute microsequence as G15 RunCompute `+0x760`: Apple publishes the SKU execution stream there.

E071 closes the exact inactive stock-empty SKU gate and the first UMA handoff byte. The relevant PerfCtr feature state and the empty descriptor input are zero, so the optional pair is absent and the stock empty path is exactly `0x2c0`. After `AGXUMAPool::prepareLocked()`, exact 23J220 `submitBuffer()` copies descriptor `+0x624` into RunCompute `+0x846`, requiring `+0x846=1`; Linux commit `f73b9e551658` corrects its previous zero. RunCompute `+0x810/+0x860/+0x870/+0x878` remain exact empty-path zeros, while `+0x85f` is dynamic and deliberately not hard-coded. Live RunCompute remains blocked on the Page-Pool/FW-uncached pointer, min/ideal/metrics values, dynamic context generation, and stamp/notifier completion/recovery ownership.

E072 closes the remaining stock-empty UMA value ambiguity and identifies the two live pointer producers. The raw empty UMA record remains zero at the qwords copied to descriptor `+0x628/+0x630`, so RunCompute `+0x847/+0x84f` are **exactly zero**; Apple's `getMinPoolSize()/getIdealPoolSize()` results govern backing allocation instead. RunCompute `+0x83e` is the live FW address of the FList-owned 0x70-byte `UMA Page Pool State`, and `+0x857` is the base FW address of the one-page `AGXUMAHWMetrics` mapping because descriptor offset `+0x620` remains zero. Linux already models the 0x70 ABI and 256-entry Page Pool descriptor table but has no G15 HardwareBuffer-ID allocator or Compute metrics owner, so those pointers remain zero/fail-closed rather than guessed.

E073 closes the managed context-ID/generation lifecycle on the exact 23J220 target. G15 has 64 context IDs with ID 0 reserved; user IDs 1..63 carry an 8-bit generation byte. Fresh assignment registers the GART then increments the generation, while sticky reuse preserves it. Linux already had the same 64-slot/GPTBAT context-ID mechanism, so commit `03fdbb86230f` adds the missing per-slot generation and exports it at RunCompute `+0x85f`. The same oracle pass proves the separate G15 HardwareBuffer manager has 0x100 IDs and owns FList mapping preparation/completion, so `+0x83e` still cannot be made live with a constant ID or naked pointer.

E074 closes the next ownership boundary for that live Page-Pool-State pointer. Exact 23J220 host reconstruction places the 0x70-byte `UMA Page Pool State` in eGartRange 8, immediately after the six already-proven range-7 parents in shared bank 1. A one-shot read-only Linux diagnostic at commit `4e26fc089860` verified shared-L2 entries 6 and 7 are empty on this exact J615, then stopped ASC and returned `ENODEV` before persistent InitData/RTKit/DRM. The machine returned to Golden with no GPU/DART/kernel fault and the experimental module/initrd restored byte-for-byte. The final range-8 leaf/PTE protection encoding remains unresolved, so no range-8 child table or `RunCompute +0x83e` pointer is published.

E075 closes that leaf/PTE encoding. The existing `0x00c0000000000447` range-7 PM leaf belongs to the ParameterManagement/IOGPUResource class; the FList SecureMemoryMap path is distinct. Exact 23J220 derives compact option `0x003` for the range-8 Page-Pool State, yielding exact leaf `0x00c0000000000443`. Linux commit `e9f50fcc17d5` published two bounded range-8 L3 parents plus one temporary leaf at `0xfffffc200c000000`, read the exact PTE back, cleared it, and detached all eight range-7/range-8 parents cleanly. ASC was stopped and the driver returned `ENODEV` before persistent runtime; no RunCompute or GPU command was issued. The persistent allocator remains range-7-only pending the next checkpoint.

E076 promotes the proven range-8 class into compile-only allocator ownership. Linux now keeps separate range-7 and range-8 `mm::Allocator` arenas beneath the same bank-1 page-table owner, tags each small-object allocator with an explicit aperture, and provides a range-8 constructor that fixes `PROT_G15_RANGE8_FW` rather than accepting PM/range-7 attributes from callers. All existing callers remain range 7 and the new constructor is intentionally unused, so this checkpoint creates no new persistent mapping and does not change the RunCompute pointer.

E077 closes the persistent FList resource constructor on exact 23J220. `AGXUMAFList::init()` creates four resources: Page Pool List and Backup Page List in eGartRange 5, a fixed 0x70-byte Page-Pool State in eGartRange 8, and a fixed 8-byte FW-Uncached State in eGartRange 7. The exact list-size formulas are `align_up(M >> 9, P)` and `align_up((M / B) * 64, P)` for pool maximum `M`, block size `B`, and host page size `P`. The corresponding G15G vslots are closed, but the override-sensitive `M/B` initialization is not, so the 4 MiB/32 KiB fallback results are intentionally not claimed as exact J615 values. A read-only stock user-client calibration (`type 5`, selector `0x10f`) succeeds but returns zero throughout the empty Compute lifecycle, proving it reports live accounting rather than those static limits.

E078 adds only the independently exact fixed mapping class. Exact 23J220 FList FW-Uncached State uses compact SecureGart option `0x00b`, giving range-7 leaf `0x00c000000000044b`; the existing PM/q22 class remains `0x00c0000000000447`. Linux commit `886820e1f460` adds an exact 8-byte `G15UMAFWUncachedState`, a dedicated FList range-7 aperture/allocator constructor, and hard aperture/protection pairing while both range-7 classes share one collision-safe VA allocator. This remains compile-only: neither FList fixed object is instantiated persistently and RunCompute `+0x83e` remains zero.

E079 reconstructs the separate 256-entry `AGXHardwareBufferIDManager` allocation state rather than substituting Linux's generic LRU slot allocator. Linux commit `865f24f2a9fc` models sticky per-object IDs, per-ID reference counts, the 256-bit zero-reference bitmap, Apple's initial LIFO stack arranged to allocate IDs 0 through 255, lowest-set-bit bitmap fallback for stealing dormant sticky IDs, stale-owner mismatch handling, and the exact prepare/complete transition booleans. The object is intentionally only a compile-time host state machine: it has no lock, no runtime instance and no FList callback wiring yet.

E080 adds the missing synchronization/host-owner boundary without activating it. Linux commit `ed17ac035ad2` wraps the exact E079 manager state in the driver's existing `Arc<Mutex<...>>` pattern and adds an FList-side sticky binding object whose prepare/complete methods expose the exact transition booleans. No manager is instantiated in GpuManager, InitData, queues or an FList resource owner, and no callback or GPU mapping is executed.

E081 adds a complete but side-effect-free FList resource plan. `G15FListGeometry` takes the unresolved exact producers `M/B/P` and computes Apple's checked formulas `align_up(M >> 9, P)` and `align_up((M / B) * 64, P)`; the fixed Page-Pool/FW-Uncached objects are compile-time pinned to 0x70/8 bytes. `G15FListResourcePlan` combines that geometry with the synchronized sticky HardwareBuffer owner but deliberately contains no GPU allocation type or allocator handle. It therefore cannot allocate, map or publish a Page-Pool State.

E082 closes the remaining FList list-mapping class on exact 23J220. The Page Pool List and Backup Page List callers use `mov x5,#3; movk x5,#0x200,lsl #32`, i.e. exact caller option `0x20000000003`; older E074/E077 prose had omitted two zeroes. Range 5 produces outer option `0x20500000007`, `SecureMemoryMap +0x58 = 0x20`, and exact compact UAT option `0x300`. Its protection bits are `0x0080000000000000`, bit-identical to the existing cached range-5 class. Linux commit `724674ad034e` adds a dedicated semantic FList constant and exposes it through the no-allocation resource plan. No allocator or GPU object is instantiated.

E083 closes the previously conditional J615 UMA geometry. Exact typed OSObject allocation is zero-filled; the complete G15 constructor, configure/start/property paths and whole-kext writer audit leave accelerator `+0x1e80/+0x1e78` untouched, and no hidden offset-table values exist. The exact G15G getters therefore select M=2 GiB and B=4 MiB. With P=16 KiB, Apple's FList formulas yield a 4-MiB Page Pool List with 0x80000 entries and a 32-KiB Backup Page List. Linux commit `b88369c26ffe` pins these constants in a checked J615 geometry constructor while preserving the no-allocation resource plan.

E084 deliberately pauses new G15 naming to cross-check current public Asahi m1n1 AGX semantics. Public m1n1 still exposes only G13/G14/G14X concepts and no public G15/T8122/J615 AGX layout. Concept-level continuity is strong for Start/Finalize-class firmware work, dual host/FW stamps plus event-control state, page-list/manager/grow bookkeeping, command context identity distinct from notifier VM state, multiple timestamp/counter domains, and queue busy/inflight/cursor state. Current public Compute statistics remain opaque. No old offset/opcode is imported: these are hypotheses to target with exact 23J220 writers, and the 23J220 oracle remains the source of truth.

E085 resumes exact-target source work and fixes a prerequisite for the future range-8 Page-Pool-State owner. The existing q22 producer previously emitted ordinary `1/0` map/unmap flags for all bank-1 addresses. Exact 23J220 requires the range-8 special 64-MiB aperture to retain bit 1 on both operations, giving map `3` and unmap `2`, while range 7 stays `1/0`. Linux commit `9b21157497bbb1695b9bbdb43d6490c23520504e` makes that distinction and extends the CPU-side preflight checks. The range-8 allocator remains uninstantiated, so this checkpoint creates no new persistent mapping.

E086 adds the persistent FList backing owner without making it reachable. Linux commit `3cd3f336d9f4` owns the exact 4-MiB Page Pool List, 32-KiB Backup Page List, 8-byte FW-Uncached State and 0x70-byte Page-Pool State through their proven mapping classes. It seeds only `pool_id` and HardwareBuffer ID `0xffffffff`, matching the `AGXUMAFList::init()` boundary; populate/prepare fields remain zero and no Page-Pool-State FWVA accessor exists.

E087 closes the exact prepare/complete residency lifetime beneath that owner. Fresh `AGXUMAFList::prepareBufferResources(true)` prepares Page Pool List, Page-Pool State, FW-Uncached State and Backup Page List in order, then on the first initialized epoch populates page-pool/firmware state. Matching IOGPUFamily proves `prepareMapping()` is a preparation/residency reference: the first nonresident reference commits PTEs, repeated references do not, and `completeMapping()` drops a reference without releasing the PTE. Actual PTE release is a separate mapping teardown. Linux can therefore conservatively retain eventual FList mappings through idle zero-reference periods, but first map/q22 publication must still be tied to HardwareBuffer activation rather than unrelated queue construction. No Linux source change or RunCompute is enabled by E087.

E088 closes the normal Compute completion ownership. Exact command-buffer construction maintains 36 rotating 0xc0-byte event-control blocks with parallel CPU/GPU address arrays; the selected GPU address is copied to CL descriptor `+0x148`, converted to FWVA by the firmware object and exported as RunCompute `+0x14`. Separately, the Compute channel emits `{stamp_value, stamp_index}` through the generic IOGPU stamp machinery, and the same stamp index is attached to the base descriptor submit event. Firmware stamp-bit notifications call `signalStamp()`, then `IOGPU::signalStampsUpdated()` kicks scheduler completion. `IOGPUWorkQueue::pruneRingBuffer()` retires a descriptor only after its generic submit event tests complete; the CL descriptor then releases context-ID and UMA/HardwareBuffer ownership before generic retained-memory completion. Thus normal ordering is stamp update → scheduler kick → event test → AGX resource retirement → queue-slot retirement. Error/recovery remains separate; no Linux source change or RunCompute is enabled by E088.

E089 applies the immediate fail-closed consequence of E088. Linux had inherited one queue-wide `fw::event::Notifier` pointer at Compute command `+0x14`; exact G15 requires the selected member of a 36-state command-buffer event-control pool instead. Linux commit `167c037a91a0b85fa10480b90e883de31fa88e0d` makes the raw field generation-specific, preserves the legacy notifier on non-G15, names the G15 field `g15_event_control_fwva_14`, and initializes it to zero until the correct rotating owner exists. The exact 0x880 command size and `+0x14` offset remain asserted. This changes no live G15 execution because submission remains fail-closed.

E090 closes exact 23J220 error/recovery retirement ownership. Firmware recovery resolves stamp index/value back to the owning WorkQueue and descriptor without freeing resources in place. The active AGX WorkQueue restart is an AGX override, not the generic IOGPU nop-loop. Its destructive branch calls `forceCompleteAllStamps()`, which advances normal stamp/event state into the E088 scheduler/prune/descriptor-complete path. Recovery then separately drains cached context/GART bindings, pending UMA grow/request HardwareBuffer/context references, the FList sticky HardwareBuffer ID, page-pool state and CL/3D channel state. Observed restart-control values `2` and `4` bypass the global forced-stamp branch; their exact enum names remain intentionally unknown. No Linux source change or RunCompute is enabled by E090.

E091 then pins the exact rotating event-control owner geometry without guessing its mapping ABI. Exact 23J220 `AGXCommandBuffer` owns 36 contiguous 0xc0-byte event-control states (0x1b00 total); the selector starts at zero and advances before selection modulo 36. Linux commit `97bd7129c3f0` adds opaque block/pool types, exact size assertions, and the host rotation helper, but deliberately implements no `GpuStruct`, allocator, mapping class or RunCompute `+0x14` publication. The next boundary is the exact 23J220 allocation/mapping class and minimum initialized fields for these blocks.

E092 closes that backing/mapping boundary on the exact target without changing Linux source. `AGXCommandBuffer::init()` uses `AGXFirmware::allocateSharedData()` for one contiguous 0x90-byte stamp pool (`36 × 4`) in normal eGartRange 7 and one contiguous 0x1b00-byte event-control pool (`36 × 0xc0`) in eGartRange 8. Their exact outer mapping options are `0x700000007` and `0x20800000007`, matching the independently proven `0x...0447` range-7 and `0x...0443` range-8 leaf classes. Construction zeroes both rounded backings and stores each corresponding stamp FWVA at event-control `+0x00`. Rotation then seeds `+0x08` a host-derived value (later closed by E095 as the unmasked shared state sequence), zero `+0x0c`, a still-neutrally-named accelerator-derived configuration at `+0x10`, zero `+0x14/+0x18`, and `-1` at `+0xa8`. The J615 numeric/semantic value of `+0x10` remains an explicit activation gate; RunCompute `+0x14` stays zero.

E093 adds the matching backing owner as unreachable compile-only code. Linux commit `ddcbd85be239` gives normal range-7 stamp/shared-event storage a hard-wired allocator class, adds an Apple-style exact-base shared-data array helper that bypasses ABI-shifting debug headers/padding and zeroes the rounded backing, and owns the 36 `Stamp` words plus 36 event-control blocks with q22 mapping-notifier lifetime. Construction seeds only each block `+0x00` stamp FWVA. The owner has no call sites, exposes no selected FWVA, and does not perform rotation-time `+0x10` setup; RunCompute `+0x14` remains literal zero/fail-closed.

E094 closes that rotation-time numeric seed without making the owner reachable. Exact 23J220 computes selected event-control `+0x10` as the PM record count capped by optional accelerator `+0x2428`; the exact normal J615 writer audit leaves the cap zero and the record-count override zero, so G15 fallback `+0x678=0x50` makes the value exactly 80. The exact RTKit type-3 handler also proves RunCompute `+0x14` is copied into per-stamp execution state, so it cannot be treated as padding. Linux commit `42bca8d2e9dc` adds an unreachable helper that can clear/reseed an already-retired selected state with stamp FWVA, the caller-supplied provisional `+0x08` value, exact `+0x10=80`, and `+0xa8=-1`; it exposes no FWVA and has no call sites. Event-machine `finishEvent()` ownership remains the next gate, and RunCompute `+0x14` stays zero.

E095 closes that reuse gate. Exact mutable `IOGPUEventMachine::finishEvent()` waits for all active stamp dependencies before resetting the host event, and `AGXCommandBuffer::nextCommandBufferState()` invokes it before clearing/reseeding the matching 0xc0 control and four-byte stamp. E095 also proves control `+0x08` is the unmasked `AGXShared` command-buffer-state sequence, not a stamp index. The raw old IOGPU host-event layout is not imported into Linux.

E096 adds a conservative compile-only Linux reuse guard. Commit `4ff63937d4fc` renames `+0x08` to `state_sequence_08` and retains the existing per-submission `JobFence` in a separate 36-slot host guard set. An unarmed fence cannot be bound, occupied slots cannot be overwritten, and a slot remains unavailable until its fence pending-command count reaches zero. The GPU backing owner remains unreachable and RunCompute `+0x14` remains zero/fail-closed.

E097 closes the exact channel-owned HWMetrics backing used by RunCompute `+0x857`. Exact 23J220 allocates one 0x4000-byte J615 host page, prepares it for the HWMetrics lifetime, zeroes it, and advances its current offset by 0x40 modulo 0x4000, giving 0x100 records. Its exact mapping option is `0x0001008700000007`; SecureMemoryMap/SecureGart reduces this range-7 variant to compact UAT `0x30b`. The plain IOBufferMemoryDescriptor direction filter does not alter that value, and bank-1 `encodePTEFlags(1, 0x30b)` emits `0x00e000000000040b`, whose protection bits are exactly Linux `PROT_GPU_FW_SHARED_RW`.

E098 promotes only that proven backing boundary into unreachable compile-only ownership. Commit `d18178f018ac` adds a distinct HWMetrics semantic aperture over the shared range-7 VA arena, hard-wires the existing uncached firmware/GPU protection shape, validates the exact `0x...040b` leaf, and owns one page-base zeroed 0x4000-byte ring with 0x40/0x100 geometry. The owner has no constructor call site and exports no FWVA; RunCompute `+0x857` remains literal zero/fail-closed.

E099 advances the other remaining pointer gate without pretending the dynamic page lists are ready. Linux commit `47a8c0070a4b` adds a definition-only first-initialized-epoch population helper for the FList 0x70 Page-Pool State. It accepts only exact host-produced dynamic inputs, validates HardwareBuffer identity and page/list bounds, and writes the proven Page/Backup/FW-Uncached FWVAs, page/ring seed, Backup List count, shared-compute state and zero tail. The actual Page Pool List / Backup Page List contents and chained UMA backing mappings remain an explicit activation prerequisite; the owner is still unreachable and RunCompute `+0x83e` remains zero.

E100 closes that list-content ambiguity for Apple's stock empty Compute command only. Exact `prepareLocked()` growth updates the pool accounting qwords read by `getDeviceUMAPoolSizes()`; a repeat stock type-5 selector `0x10f` calibration remained `0,0,0,0` before/after queue creation, encoding, successful completion and a one-second hold. Therefore the fresh empty FList takes no nonzero `allocatePoolMemory()` path, `FList +0x68/+0x70/+0xc0` remain zero, and `populatePagePool()` leaves both list backings zero-filled. Linux commit `bd95b9c01004` adds an unreachable helper that verifies those zero lists and populates the E099 state with zero page count/cursors and zero Backup extents while preserving the real mapped resources, capacity `0x80000`, queue-derived priority and HardwareBuffer ID. RunCompute `+0x83e` remains zero/fail-closed.

E101 closes the stock-empty SKU byte sources on exact 23J220 rather than relying on the earlier packet grammar alone. The inactive stream is `0x2c0`; the type-0xb payload is zero-initialized and then populated from the command, selected event-control/state sequence, Page-Pool State, HWMetrics, context/UUID/stamp state and the exact J615 CL channel-resource regions. It corrects two earlier prose ambiguities: descriptor `+0x460` maps to setup payload `+0x3c`, while payload `+0x40` is JobParameters2; timestamp record `+0x34` is the command UUID. Type-0xc setup-payload backpointer is `stream_fwva+0x15c`, and its exact negative packet-start bookkeeping is `0xfffffdc8`. No Linux source changes occur in E101.

E102 adds the corresponding compile-only serializer at Linux commit `897d3ae41896`. It fixes the exact packet offsets and J615 `0xf400`/`0x800` channel-region geometry while requiring every live pointer/value as an explicit input. The serializer owns no GPU allocation, has no call sites outside its definition, and the existing RunCompute `+0x760` construction path remains unchanged. Build/checkpatch and an independent layout/source invariant audit pass; no module is installed and no GPU command is issued.

E103 closes the exact SKU backing ownership on 23J220. The normal J615 CL command pool has `0xf0` usable entries; `AGXSKUEncoder::createBuffer()` receives block size `0x300` and count `0xf0`, giving `0x2d000` logical bytes and an exact page-rounded `0x30000` GPU backing. The encoder also owns a separate `0x3c00` host event array for slot reuse. Its GPU mapping uses exact option `0x20800000007`, the already-live-proven range-8 `0x00c0000000000443` leaf class; `createBuffer()` does not establish a per-command prepare/complete epoch and `free()` owns final backing teardown. No Linux source changes occur in E103.

E104 adds the matching unreachable Linux owner at commit `d54dfc78d059`. It pins `0xf0 × 0x300`, allocates one exact-base zero-filled `0x30000` range-8 backing with q22 notifier lifetime, and deliberately provides no selected-slot/FWVA accessor. The E102 serializer remains disconnected and RunCompute `+0x760` is unchanged. Build/checkpatch pass at the established baseline; no module is installed and no GPU command is issued.

E105 closes the exact slot-selection/retirement protocol. `AGXSKUEncoder::beginEncoding()` starts at the next index modulo `0xf0`, scans no more than all slots with `IOGPUEventMachine::testEvent()`, and copies the CL producer's command-associated event into the first reusable host slot before assigning its CPU/GPU offset. `finishEncode()` converts GPU backing base + `slot*0x300` and returns it to RunCompute `+0x760`; `scrubEvents()` only cleans completed host events. No Linux source changes occur in E105.

E106 adds an unreachable conservative Linux slot guard at commit `69af01482ad8`. It uses `JobFence.pending==0` as the completion predicate, preserves Apple's exact next-slot scan order, binds the new fence before exposing the slot index, returns `ENOSPC` if all slots are busy, and can scrub completed guards without moving the rotation point. A whole-submission fence is deliberately stronger than Apple's command-local event. It has no call sites and does not write the GPU SKU backing or alter RunCompute `+0x760`.

E107 adds the still-unpublished copy/token boundary at Linux commit `f3464e7ec216`. Given a caller-proven retired slot, the E104 backing clears that entire 0x300-byte slot, copies the exact E102 0x2c0 bytes, verifies the final 0x40 bytes remain zero, computes backing FWVA + `slot*0x300`, and returns a prepared token with index/FWVA/size. The writer/token have no call sites, the backing/guard remain unreachable and RunCompute `+0x760` remains unchanged.

E108 proves those independently closed prerequisite owners can coexist without collapsing their Apple lifetimes. Linux commit `ea707e8e7726` owns event-control backing, HWMetrics, FList/HardwareBuffer state and the SKU backing beneath one definition-only graph sharing bank-1/q22 ownership; the per-VM range-5 FList allocator remains separate. The graph performs no slot selection, FList activation, HWMetrics advance or SKU copy and exposes no FWVA. RunCompute `+0x14/+0x83e/+0x857` remain zero and `+0x760` remains unchanged.

E109 advances that graph to one coherent but still unpublished command-assets token. Linux commit `a783f835b437` accepts caller-proven retired event/SKU indices, reseeds/copies those exact backings, activates the stock-empty FList HardwareBuffer reference with first-epoch population and rollback, takes the current HWMetrics record, and returns a private non-Copy token carrying event-control/SKU/Page-Pool/HWMetrics FWVAs, SKU size and HardwareBuffer ID. Completion consumes the token. The E096/E106 guards remain separate; the token has zero outside-module call sites and no RunCompute conversion.

E110 closes that guard-selection boundary in definition-only code. Linux commit `63bb6ffe4f22` requires an already-armed `JobFence`, tests the exact next 36-state event-control slot without advancing on `EBUSY`, binds it, scans/binds the first reusable `0xf0` SKU slot, and only then calls E109. Newly bound guards roll back on failure; FList reference rollback remains in E109. The wrapper has zero Queue/submission call sites, so command fields remain unchanged.

E111 converts the resulting guarded private assets into a host-only field staging record rather than mutating a firmware command. Linux commit `139b745dbf00` stages the exact stock-empty command-facing values for event-control `+0x14`, SKU `+0x760/+0x768`, Page-Pool State `+0x83e`, UMA `+0x846/+0x847/+0x84f = 1/0/0`, and HWMetrics `+0x857`, while retaining the non-Copy HardwareBuffer asset token until staged completion. It also rejects zero/invalid staged pointers and wrong SKU size. There is no method that writes the stage into `RunCompute`; the live constructor still emits zero for `+0x14/+0x83e/+0x857` and retains its existing `+0x760` path.

E112 audits the remaining integration gap without changing Linux source. Exact 23J220 submission fills the command before register generation, then encodes SKU from the known command FWVA and selected SKU slot; the current dormant layers instead accept a prebuilt SKU before either address relationship can be finalized. The audit therefore requires a two-phase select/prepare → command-aware finalize transaction. It also catches a fence-order mismatch (`submit_compute()` currently constructs before `fence.add_command()`), missing serializer bridges for firmware/channel/region/soft-fault/accelerator/event-index state, and still-zero Apple-active pre-micro/JobParameters2 values. Direct E111 → RunCompute mutation is explicitly blocked.

E113 implements only that required two-phase dormant transaction. Linux commit `b21300a1ad4d` adds an address-only `G15ReservedSkuSlot`; phase 1 binds the existing event/SKU guards, seeds event-control, reserves the SKU slot without touching its bytes, activates the stock-empty FList/HardwareBuffer epoch, and reserves Page-Pool-State/HWMetrics addresses. Phase 2 requires a nonzero future command FWVA plus explicit firmware/channel/CL-region/stamp/context/event-index/soft-fault/accelerator inputs, then constructs E102 and writes only the revalidated reserved slot. Abort/finalize failures complete the FList reference and roll fresh guards back. No Queue call site or RunCompute writer is added.

E114 closes one of those explicit runtime sources on the exact 23J220 target. `AGXCLChannel::init()` derives a J615 per-region stride of `0xf400`, one-MGPU span `0x800`, and one persistent logical `0x1f400` CL command resource in normal option-3 eGartRange 5. The same submit-side audit proves Compute `JobMeta.evctl_index` comes from `AGXChannel +0x38` and `flush_stamps` is `(descriptor +0x48 != 0)`. E114 is static-only and changes no Linux source.

E115 promotes only the CL resource ownership boundary. Linux commit `059d34701e48` pins the exact geometry, adds an unreachable range-5-uncached `G15ClCommandResourceBacking`, attaches it only to the existing definition-only owner graph, and removes `channel_command_region_base_fwva` from the dormant finalize-input list by deriving it from the owned resource. The graph still has no Queue/submission caller; live G15 RunCompute `+0x14/+0x83e/+0x857` remain zero and the live `+0x760` producer is unchanged.

E116 closes the next dormant SKU address source on exact 23J220. Firmware initializes a global `_AGFIChannelState` resource stack with 0x24c0-byte elements in eGartRange 8. The stack allocates exact 0x8000-byte page-base blocks, fitting three states at offsets 0x0000/0x24c0/0x4980; its exact map option is `0x20800000007`, compact UAT `0x003`, leaf `0x00c0000000000443`. `AGXChannel::init()` stores `block_gpuva + slot_index * 0x24c0` at channel `+0x90`, and `resetChannelState()` clears the full selected state before rebuilding its first 0xb0-byte QueueInfo.

E117 promotes only that backing geometry. Linux commit `252ded3b6353` pins the exact element/block/slot/slack constants and adds one unreachable special-range-8 `G15ChannelStateBackingBlock`. It deliberately exposes no selected-slot FWVA, does not model allocation/release or QueueInfo reset, and has no constructor caller. Dormant `channel_state_fwva` therefore remains explicit and live RunCompute is unchanged.

E118 closes the selected-state host reset and priority-mutation boundary without changing Linux source. Exact `resetChannelState()` clears the full 0x24c0 slot and rebuilds QueueInfo with reset priority class 4, `+0x2c/+0x4c=-1`, channel config at `+0x50`, converted GpuContext at `+0xa4`, and J615 CDM backoff byte 4 at `+0xac`. Exact `setPriority()` later rewrites `+0x30/+0x34/+0x38/+0x40/+0x44/+0x48`; the inherited Linux `raw::PRIORITY` table cannot be treated as an exact G15 lookup. The normal CL constructor first integer (SKU `evctl_index`) and priority setter tuple remain open, so E117 stays disconnected.

E119 closes the ordinary CL constructor/priority source chain without changing Linux. `chooseCLWorkQueue()` passes queue `+0x8a0` and the pre-increment CL-workqueue index `+0x890`; matching IOGPU/AGX workqueue constructors preserve them at `+0x54/+0xe0`, and `AGXCLChannel::init()` receives them as its second/first integer arguments. Therefore SKU `evctl_index` is exactly the CL-workqueue index (0 for the first channel) and the normal-J615 second integer is `0x50`. The priority setter tuple is `(effective IOGPU priority, 2, priority == 1 ? queue_qos : 2)`; the accelerator remap table is exact-zero on normal J615. Runtime foreground/QoS remains an explicit host input rather than a guessed constant.

E120 promotes only that exact selected-state host boundary into unreachable compile-only code. Linux commit `98d829e05a89` keeps the proven 0x8000/three-slot backing, clears exactly one selected 0x24c0 state, rebuilds the E118 reset QueueInfo image, and applies the E118/E119 normal-J615 priority mutation from explicit effective-priority/QoS inputs. The resource-stack slot index is intentionally not reused as channel `evctl_index`; the latter is a separate CL-workqueue constructor value. The helper returns only a private prepared-state token and has no conversion to SKU input, RunCompute or a live channel. Build/checkpatch/layout validation pass; no module is installed.

E121 closes the selected QueueInfo `+0x18` producer. Exact `AGXChannel::init()` stores selected-state GPUVA + `0xb0` at channel `+0x88`; exact reset converts that value and publishes it at QueueInfo `+0x18`. Because `0xb0` is exactly the G15 QueueInfo size, this pointer starts immediately after QueueInfo inside the same 0x24c0 slot. Linux commit `563b93f4bce3` removes `gpu_buf_fwva` from the private reset inputs and derives it from the selected slot, while deliberately leaving the inherited live WorkQueue gpu-buffer allocation untouched.

E122 closes the two selected channel-memory resources feeding QueueInfo `+0x00/+0x08`. Exact 23J220 firmware owns separate `AGXUncachedFWChannelMem` range-7 and `AGXCachedFWChannelMem` range-8 stacks. On normal J615 both use exact `0x2860` elements and page-rounded `0x8000` backing blocks containing three elements at `0/0x2860/0x50c0`; the mapping options reduce to the already-proven range-7 leaf `0x00c0000000000447` and range-8 leaf `0x00c0000000000443`. Channel init stores selected GPU addresses at `+0x98/+0xa0`; reset publishes them at QueueInfo `+0x00/+0x08`; channel free returns each selection through its own resource-stack lifetime.

E123 promotes only those exact backing boundaries into compile-only Linux. Commit `14954ffdd9c5` adds hard geometry constants, dedicated semantic range-7/range-8 channel-memory allocators, and one independent unreachable `0x8000` owner for each stack. Neither owner exposes a selected element FWVA, neither is in the dormant owner graph, and live WorkQueue/RunCompute producers are unchanged.

E124 closes the first selected uncached-channel-memory host initialization. The exact CL second constructor integer `0x50` becomes channel `+0x54 = min(0x50,0x80)<<4 = 0x500`; `resetChannelState()` then writes uncached element u32 words `+0x00/+0x10/+0x20/+0x30/+0x40 = 0` and `+0x50 = 0x500`, without bulk-clearing that element or touching cached channel memory. Linux commit `1d9c455c0c0d` models only those six writes on a caller-selected local slot and returns no address.

E125 corrects selected QueueInfo `+0x10`. Exact command queue `+0x828` is `AGXTimeStampQueue`; `chooseCLWorkQueue()` passes its selected state GPUVA from object `+0x28` as the fifth CL-channel constructor argument, base channel init stores it at `+0xa8`, and reset publishes its firmware address at QueueInfo `+0x10`. The timestamp resource stack is exact range 7 with 0x18-byte states, 0x4000 backing blocks, 0x2aa states/block and 0x10 slack; the mapping is the established `0x700000007` / leaf `0x00c0000000000447` class. Linux commit `adfbfca2e265` renames the private input accordingly and models one unreachable timestamp backing/reset image, but deliberately leaves the inherited live WorkQueue `notifier_list` constructor path unchanged.

E126 closes selected QueueInfo `+0xa4` provenance. Exact firmware owns `_AGFISchedulerState` at resource stack `+0xdb8` with eGartRange 8 and 0x40-byte elements; the generic initial backing rounds to one 0x4000 page, exactly 0x100 states with no slack. Command-queue construction stores the selected GPUVA at `+0x8d8`, its converted FWVA at `+0x8d0`, channel init copies it to `+0xf0`, and reset publishes it at QueueInfo `+0xa4`. Apple host-reset clears exactly the first 0x38 bytes and writes the already-recovered G15 scheduler-state seed, leaving `+0x38..+0x3f` untouched. Linux commit `293ca6338017` adds only an unreachable semantic range-8 owner/reset and renames the dormant `gpu_context_fwva` input to `scheduler_state_fwva`; the live generic GpuContext allocation/release path is unchanged.

E127 integrates the exact E116-E126 channel prerequisites into the existing dormant stock-empty Compute owner graph without creating a live channel. Timestamp, scheduler, channel-state, uncached channel-memory and cached channel-memory stacks keep independent local indices; the private phase-1 model derives QueueInfo `+0x00/+0x08/+0x10/+0x18/+0xa4` from those owned selections and preserves the existing event/FList/HWMetrics/SKU lifetimes. Linux commit `8f6bd394ceb5` removes external `channel_state_fwva` from the command-aware SKU finalize inputs and instead feeds the internally prepared channel-state token. Timestamp mode, queue `+0x4c`, effective priority/QoS and other still-unbridged command values remain explicit. No Queue call site or RunCompute writer exists.

E128 closes two fixed stock-empty SKU values for the first normal J615 CL channel. E119 already proved that channel's `AGXChannel +0x38` / G15 `JobMeta.evctl_index` is zero. A fresh exact 23J220 packed-feature audit also proves accelerator halfword `+0x654` bit 7 (overall bit 39) is zero: base configure mask `0xf4840fffffff7f` clears it and all later base/G15/G15G transformations preserve zero, including G15G OR `0x2002100000180`. Linux commit `ccc976c598ab` removes both values from the dormant finalize input structure, uses the exact first-CL constant and hard-wires the target accelerator bit to false only at the J615 owner-graph boundary. The generic serializer remains parameterized. No live producer changes.

E129 closes the remaining GART HW-soft-fault boolean at the same dormant boundary. Exact 23J220 userspace initializes IOGPU device options to zero and constructs user-client type `5 | (options << 16)`, so the ordinary path is type `0x00000005`; kernel initialization propagates that zero through `IOGPUDeviceUserClient +0x128`, `IOGPU::createDevice()`, `AGXShared +0xd8` and `AGXSecureGart`. Together with the already-proven normal J615 accelerator soft-fault feature state of zero, `AGXGart::isHWSoftFaultEnabled()` is exactly false. Linux commit `65451e3d5cc8` removes `gart_soft_fault_enabled` from the target-specific finalize input and hard-wires false only at the J615 owner-graph boundary; the generic serializer remains parameterized. No live Queue or RunCompute producer changes.

E130 closes `firmware_state_fwva` ownership at that dormant boundary. Exact `AGXFirmware::allocFirmwareData()` descriptor index 3 pairs host `+0x228` with GPU/FW-visible `+0x268`, size `0xe10`; its position in the first size sequence `0x1860, 0xc10, 0x1248, 0xe10, 0x4360` identifies it as the Compute statistics object between Fragment stats and HwDataA. `AGXFirmware::initFirmwareData()` zeroes exactly all `0xe10` bytes, `AGXArmFirmware::initFirmwareSharedData()` converts `+0x268` into shared-data `+0x244`, and the G15 CL SKU encoder consumes the same source. Linux already owns the exact-size/default-zeroed `G15StatsComp` under manager-global RuntimePointers. Commit `635fca8e84b3` removes the arbitrary raw finalize address and carries a typed `GpuWeakPointer<G15StatsComp>` only in the dormant owner graph; the generic serializer remains parameterized and no live Queue or RunCompute producer changes.

E131 closes the remaining command-local scalar sources at the same dormant boundary. Exact 23J220 SKU encoding reads the command counter from RunCompute `+0x04`, managed context ID from `+0x10`, JobMeta FW stamp/value from `+0x7f0/+0x7f8`, UUID from `+0x808`, and user-timestamp presence from `+0x828/+0x830`; E114 independently proves descriptor `+0x48c`, used by the setup packet, is copied into RunCompute `+0x80c` as `G15JobMeta.event_seq`. Linux commit `251a60a085f3` therefore deletes `G15StockEmptySkuFinalizeInputs` and derives these values from the typed already-initialized `RunComputeG15V14_7` image. G15 `+0x10` is renamed to `g15_context_id_10` without changing its `VmBind::slot()` producer or layout, and the generic serializer's user-timestamp contract is narrowed to the exact presence predicate. The serializer remains parameterized; no live Queue/RunCompute writer is added.

E132 recovers the exact macOS 14.8 / 23J220 `AGXMetalG15G_C0` userspace producer from the matching OTA system Cryptex and closes the stock-empty userspace-produced command body without a Linux delta. Exact `beginComputePass()` zeroes the complete 0x1d0 raw Compute payload; the no-dispatch begin/end path never rewrites raw `+0x008/+0x010/+0x018/+0x020/+0x028/+0x030/+0x038/+0x040`, proving RunCompute `+0x774/+0x77c/+0x784/+0x78c/+0x7a4/+0x7ac/+0x7b4/+0x7bc` are all zero. Exact kernel repacking plus userspace constructor/empty-finalizer defaults also prove Compute `G15JobMeta.engine_state` at `+0x7e4` is zero. The same producer writes raw `+0x0a4=0x1c`, unlike the newer retained capture, directly validating the same-build guard. Linux E131 already has the exact zero values. E133 subsequently corrects the then-open pre-micro source attribution and closes those values as described below.

E133 replays the exact 23J220 `processSegmentKernelCommand -> processCompute -> processComputeSetup` register chain and corrects an older alias error: the pre-micro snapshot register is the parsed `AGXComputeHardwareKernelCommand` wrapper, not `AGXCommandQueue this`. Exact `parseAndValidate()` maps wrapper `+0x20 <- raw Compute +0xc0` and wrapper `+0x38 <- raw +0xd8`; the existing descriptor/submit path therefore maps those qwords to RunCompute `+0x740` and split `+0x748/+0x750`. The matching E132 userspace producer explicitly zeros raw `+0xc0/+0xd8`, and the exact no-dispatch finalizer does not rewrite them, proving all three stock-empty command values are zero. Linux commit `f09bed89530e` changes only the semantic field names/comments and preserves the bytes and hard offset locks. Patch 0049 is compile-only; live RunCompute remains fail-closed.

E134 closes the fence-order prerequisite identified by E112 without making the dormant transaction live. Private versioned `G15CommandFenceArm` clones the existing submission `UserFence<JobFence>`, increments `pending` exactly once on construction and decrements through the existing `command_complete()` on Drop. Phase 1 now creates that arm before event-control/SKU retirement binding, and the arm is carried through private armed prepare/finalized-stage tokens. Existing failure/abort paths explicitly roll event/SKU/FList ownership before the arm is released; explicit completion releases the FList HardwareBuffer epoch before dropping the arm. Linux commit `a244a846e301` changes only this unreachable two-phase lifetime model: ordinary `submit_compute()` still uses its existing fence sequence, there is no Queue call site, no RunCompute writer, and no command byte changes. Patch 0050 is compile-only.

E135 audits where those dormant persistent owners can actually live and finds a critical lifetime split. Exact `AGXAcceleratorG15::start()` initializes the 0x100-entry `"UMAPool"` HardwareBuffer-ID manager at accelerator `+0x2a08`; `AGXUMAPool::prepareLocked()` uses that same accelerator-global manager with the pool FList as sticky owner. Exact `AGXChannel::init()` separately selects a normal CL pool from an `AGXUMASharedPoolContainer` using `((DataMasterType == 2) ? 2 : 0) + priority_class`, reusing a retainable existing pool or creating/inserting a shared+async-grow pool before retaining it at channel `+0x188`. `AGXUMAPool::init()` assigns pool `+0x80` from a global monotonically incremented host counter whose image starts at zero, so a particular Compute pool ID depends on earlier UMAPool creation and is not a target constant. The current all-in-one dormant graph therefore cannot be instantiated once per Linux Queue without inventing a per-queue ID namespace and unique FList where Apple has accelerator-global ID state and shared/reusable Compute pools. E135 makes no Linux source change; the next safe step is a definition-only pool/channel ownership split.

E136 performs that definition-only split without placing any owner live. `G15DeviceUmaOwnerState` groups one exact 0x100-entry HardwareBuffer-ID manager with the zero-initialized global pool-ID creation sequence, while typed `G15UmaPoolIdentity` values remove arbitrary raw pool IDs from the pool-construction boundary. `G15SharedComputeUmaPoolOwner` owns one reusable priority-class Compute pool/FList and can only source its manager and pool identity from that device-global state. The former `G15StockEmptyComputeOwnerGraph` becomes `G15StockEmptyComputeChannelOwners` and loses the FList, manager, raw pool ID, owner cookie and FList list allocator entirely; dormant prepare/finalize/rollback/completion paths receive the shared pool separately. Linux commit `7c7e4d68082f` changes only unreachable ownership scaffolding; `GpuManager`, `queue/compute.rs`, ordinary `submit_compute()` and every RunCompute byte remain unchanged. Patch 0051 is compile-only. The remaining placement boundary is exact shared-pool-container retain/select/replacement semantics plus eventual single device-global UMA placement after TA/3D/CL creation all share one pool-ID sequence.

E137 closes that shared-pool-container lifetime without a Linux delta. Exact `0xfffffe000aa15238` is a nonzero-only atomic reference acquisition: it CAS-increments object refcount `+0x0c` only if nonzero and returns boolean success, so the four container pool slots are weak references promoted under container lock `+0x40`. A failed promotion is replaced in-place. A newly shared UMAPool stores/retains its container at pool `+0xe0`; `AGXUMAPool::cleanup()` releases that reference, while `AGXUMASharedPoolContainer::free()` never traverses/releases pool slots. Separately, channel assignment increments pool `+0x20`; its `0 -> 1` transition retains the pool once and links it on accelerator `+0x410`, while `AGXAccelerator::removeUMAPool()` performs the `1 -> 0` unlink/release and channel teardown separately releases its direct `+0x188` pool reference. Last-reference `AGXUMAPool::finalize()` clears a container slot only if pointer identity still matches the finalizing pool, making concurrent zero-ref replacement safe. A Linux container therefore cannot simply own strong pool `Option`s; an equivalent weak-slot/reference state machine is required before E136 becomes live.

E138 closes the device-global host-state placement boundary before modeling that container. Exact `AGXCommandQueue::init()`, base/CL/3D WorkQueue initialization create no channels or UMAPools. `AGXCommandQueue::chooseCLWorkQueue()` invokes exact J615 accelerator `newCLChannel` vslot `+0xb48` only when an absent CL workqueue is created; `AGXCommandQueue::allocate3DWorkQueue()` similarly reaches exact `newTAChannel +0xb38` and `new3DChannel +0xb40` only on the requested 3D path. Pool construction is therefore lazy and the global pool-ID source remains at zero until an actual channel/pool creation path runs. Linux commit `f2cb03001da0` places one G15-only `G15DeviceUmaOwnerState` under `GpuManager`, containing the exact 0x100 HardwareBuffer-ID namespace and zero-seeded pool-ID sequence. Construction creates no UMAPool, consumes no pool ID, publishes no GPU address and exposes no Queue accessor; the only increment path remains the dormant shared Compute pool constructor. Patch 0052 is host-only placement, not RunCompute enablement.

E139 closes the FList range-5 address-space provenance without changing Linux. Exact `AGXShared::init()` owns one `AGXUMASharedPoolContainer` at `AGXShared +0x1b8`, while the inherited shared-object state carries the client `task *` at `+0x50` and `AGXShared::createUserGPUTask(task *)` result at `+0x58`. `AGXUMAPool::init()` and `AGXUMAFList::init()` propagate that client IOGPUTask into FList `+0x38`; both persistent range-5 Page/Backup List mappings use it, even though the Backup List uses a kernel/global backing-task argument. The 0x70 range-8 Page-Pool State and 8-byte range-7 FW-Uncached State instead pass no client IOGPUTask and remain shared-bank-1 resources. Therefore Linux’s existing per-`file::Vm` range-5 allocators are the correct FList address-space source. The remaining shared-pool selection state must be scoped to a client VM/AGXShared analogue rather than one instance per Queue or one accelerator-global pool. Live RunCompute remains fail-closed.

E140 places that client-lifetime container state without making any pool live. Exact `AGXShared::free()` releases and zeros `+0x1b8`, while `AGXUMASharedPoolContainer::free()` performs no pool-slot traversal/release, reinforcing E137’s weak-slot ownership. Linux commit `e48a1f854d3a` adds exactly four non-owning optional `G15UmaPoolIdentity` slots under one mutex/Arc stored in each G15-capable `file::Vm`, adjacent to its existing range-5 allocators. The container has no Queue accessor or promotion API, consumes no global pool ID and allocates no UMAPool/FList. Patch 0053 is host-only placement; E137’s safe weak promotion/direct-reference contract remains the next integration prerequisite.

E141 models the E137 weak-slot promotion/direct-reference contract in safe Rust without introducing a strong pool slot. The in-tree kernel `Arc` has no weak-reference facility, so each client container slot continues to hold only a `G15UmaPoolIdentity`, now with separate logical channel-direct and active-channel lifetime state. Compute selection uses exact slots `2 + priority_class`; promotion occurs under the client-container mutex only while the identity is logically live. The RAII channel reference owns the client-container `Arc`, drops the active contribution before the direct reference, and clears only the same identity at final zero. Linux commit `a301b5b72fef` remains definition-only: the per-VM container is still not exposed to Queue creation, no actual shared-pool owner is stored, no pool ID is consumed and no RunCompute producer changes. Patch 0054 closes the safe promotion state machine; actual pool-owner coupling/selection-create replacement remains gated.

E142 closes the selection/create serialization required before that safe promotion model can own a real shared Compute pool. Exact `AGXChannel::init()` ARM64 takes the shared-container `+0x40` lock before slot lookup/try-retain and does not release it across failed promotion, `halNewUMAPool()`, shared/async UMAPool initialization, nested accelerator-list insertion, slot publication and container retain; unlock occurs only immediately before channel `+0x188` is published. Linux commit `594d39b1b85e` therefore couples `G15SharedComputeUmaPoolOwner` to the client slot only through an explicit logical lifetime state machine and executes its caller-supplied create closure while the same client-container mutex remains held. Final zero removes the owner under lock and runs FList/GPU destructors only after unlocking. The per-VM field still has no Queue accessor and the constructor has zero live callers, so patch 0055 does not create a pool, consume a pool ID or change RunCompute.

E143 carries that client shared-pool context to the future lazy channel boundary without selecting a pool. Exact `AGXChannel::init()` loads its owning command queue `+0x488` shared object and then `AGXShared +0x1b8` to obtain the container, confirming command-queue lifetime retains the same client context used later by channel construction. Linux commit `848a9d426b2d` clones the existing per-`file::Vm` container at `queue_create()`, passes it through `GpuManager::new_queue()` and retains it privately in G15 `QueueInner`. No E142 selection/create method is called, the shared Compute pool constructor still has zero callers and `allocate_pool_identity()` remains reachable only from that dormant constructor. Patch 0056 therefore consumes no pool ID, creates no UMAPool/FList and changes no Compute/RunCompute producer.

E144 retains the rest of the exact context required by that future lazy Compute-channel transaction. Linux commit `6c65dc3a73c6` clones the per-client range-5 uncached/cached allocator handles plus shared-bank-1 and q22 mapping-notifier handles before existing Queue initialization consumes them, and stores the clones privately beside the E143 shared-pool container. These are the inputs required by the dormant shared UMAPool/FList and channel/command owners, but no selector or constructor is called: both constructors still have zero live callers, no global pool ID advances and `queue/compute.rs` is unchanged. Patch 0057 narrows the eventual activation boundary to actual Compute-command preparation rather than eager generic Queue/WorkQueue construction.

See `research/g15/G15-23J220-COMPUTE-ABI.md`, `research/g15/G15-23J220-COMPUTE-REGISTERARRAY.md`, `research/g15/G15-EMPTY-COMPUTE-REGISTERARRAY.md`, `research/g15/G15-EMPTY-COMPUTE-CONTAINER.md`, and `research/g15/G15-23J220-COMPUTE-SKU-SOURCES.md`.

## E061 — first real Compute is an execution boundary

Static E061 reconstruction identifies the previously unlabelled RTKit Compute launch hook at `0xfffffc00000251e8..0x256db`. After type-3 parser/DAG binding it registers RunCompute `+0x83e` as the UMA Page Pool State, propagates context-generation/selector state, then copies RunCompute `+0x760` into the per-stamp execution record at `+0x28/+0x30`. The common scheduler marks the command dispatched only after this hook returns.

Apple's matching producer is equally explicit. `AGXCLChannelG15::generateRegisterList()` builds the real G15 CDM register list, while `encodeCLCommandSKUStream()` emits a leading type-`0xb` SKU packet whose payload references the command RegisterArray (`+0x20`) and JobParameters2 (`+0x76c`), surrounds Compute WFI with timestamps, and publishes the final stream at `+0x760/+0x768`. Therefore a normal Apple-style RunCompute cannot be treated as a parser-only diagnostic; it is already hardware-facing.

RTKit's UMA registration accepts a zero-page descriptor, but the 0x70-byte Page Pool State must be real and later refresh requires a valid FW-uncached-state pointer/mirror. The next live command remains blocked until the minimum G15 register, SKU, UMA, notifier/context, stamp/completion, and recovery contracts are closed together.

See `research/g15/G15-COMPUTE-LAUNCH-BOUNDARY.md`.

E145 assembles the previously separate J615 owner tiers into one private unpublished Compute-channel lifetime without enabling it. `GpuManager::g15_select_or_create_compute_pool()` preserves E142's client-container transaction while connecting replacement construction to the device-global UMA namespace plus cached range-5 FList allocator. `QueueInner::g15_assemble_unpublished_compute_channel()` then constructs the separate channel/command resources from E144's retained uncached range-5, bank1/q22 and manager-owned `G15StatsComp` inputs only after pool selection returns. The Linux-only HardwareBuffer owner cookie is derived from the globally ordered nonzero pool identity rather than accepted as an arbitrary input. The assembly helper has zero callers, `queue/compute.rs` is unchanged, and live pool creation/RunCompute remain fail-closed. Patch 0058 is compile-only assembly.

E146 closes the ordinary J615 shared Compute UMAPool priority class. Exact `AGXChannel::init()` copies command-queue effective context priority `+0x44c` to channel `+0x1e8`, then maps only context types 0/5 to class 0 and all other types to class 1. Matching IOGPU/AGX state initializes type 3 and normal runtime branches select types 1/2, so the normal CL/Compute path always selects class 1. Linux removes the raw class parameter from the dormant E145 pool/assembly bridge and uses `G15_J615_NORMAL_COMPUTE_POOL_PRIORITY_CLASS = 1`; the helper still has zero callers and `queue/compute.rs` remains unchanged. Patch 0059 is compile-only exact-input closure.

## Source checkpoint

The current clean Linux checkpoint head is:

`a244a846e3014b9eebdec580f852e39d27a1a50d`

Patch 0004 remains the squashed delta through the scheduler-registration boundary. Patch 0005 is the focused compile-only E068 delta from `2f08f68` to `1d264651`, adding only the exact stock empty-Compute G15 RegisterArray and its `0x1a440` mirror. Patch 0006 is the focused E071 delta from `1d264651` to `f73b9e55`, correcting only the exact stock empty-Compute UMA prepared byte at RunCompute `+0x846` from `0` to `1`. Patch 0007 is the E073 delta from `f73b9e55` to `03fdbb86`, wiring the exact managed context-generation byte to RunCompute `+0x85f`. Patch 0008 is the E074 one-shot read-only range-8 parent-ownership diagnostic from `03fdbb86` to `4e26fc08`. Patch 0009 is the E075 range-8 leaf-PTE proof from `4e26fc08` to `e9f50fcc`. Patch 0010 is the E076 compile-only allocator split from `e9f50fcc` to `6e3850dc`. Patch 0011 is the E078 compile-only FList range-7 state delta from `6e3850dc` to `886820e1`, adding the exact `0x...044b` class and 8-byte FW-Uncached ABI without instantiating persistent FList resources. Patch 0012 is the E079 compile-only HardwareBuffer-ID state delta from `886820e1` to `865f24f2`, modeling Apple's exact 256-ID sticky/refcount/free-state behavior without a runtime manager. Patch 0013 is the E080 compile-only ownership wrapper from `865f24f2` to `ed17ac03`, adding kernel synchronization and an FList-side sticky binding while still creating no runtime instance. Patch 0014 is the E081 compile-only FList resource plan from `ed17ac03` to `affdd1fb`, adding exact symbolic geometry and fixed-object assertions without any GPU allocator. Patch 0015 is the E082 compile-only range-5 list-class delta from `affdd1fb` to `724674ad`, pinning exact compact `0x300` under a dedicated FList semantic protection constant without allocating GPU memory. Patch 0016 is the E083 compile-only J615 geometry delta from `724674ad` to `b88369c2`, promoting exact M/B/P and 4-MiB/32-KiB list sizes into the side-effect-free FList plan. Patch 0017 is the E085 compile-only q22 delta from `b88369c2` to `9b211574`, encoding exact range-8 special-aperture map/unmap flags `3/2` while preserving range-7 `1/0`. Patch 0018 is the E086 compile-only FList ownership delta from `9b211574` to `3cd3f336`, owning all four exact persistent backings while exposing no command pointer. Patch 0019 is the E089 compile-only event-control delta from `3cd3f336` to `167c037a`, removing the wrong-lifetime legacy notifier from G15 RunCompute `+0x14` and leaving the exact event-control FWVA zero until its rotating owner exists. Patch 0020 is the E091 compile-only event-control-pool delta from `167c037a` to `97bd7129`, pinning only the exact 36 × 0xc0 rotation geometry while leaving mapping and publication absent. Patch 0021 is the E093 compile-only backing-owner delta from `97bd7129` to `ddcbd85b`, owning the exact range-7 stamp and range-8 event-control shared backings at page-base logical offsets while keeping rotation and `+0x14` publication unreachable. Patch 0022 is the E094 compile-only selected-state delta from `ddcbd85b` to `42bca8d2`, pinning exact normal-J615 event-control `+0x10=80` and the post-finish reset/seed image while exposing no selected FWVA. Patch 0023 is the E096 compile-only reuse-guard delta from `42bca8d2` to `4ff63937`, correcting `+0x08` to the shared state sequence and preventing rotated slot reuse until the retained submission fence is complete. Patch 0024 is the E098 compile-only HWMetrics-owner delta from `4ff63937` to `d18178f0`, adding the exact `0x...040b` range-7 HWMetrics semantic class and one unreachable page-base 0x4000-byte/0x40-record ring owner while leaving RunCompute `+0x857` zero. Patch 0025 is the E099 compile-only FList population delta from `d18178f0` to `47a8c007`, modeling the exact post-`populatePagePool()` 0x70 state image while keeping page-list contents, chained backing mappings and RunCompute `+0x83e` gated. Patch 0026 is the E100 compile-only stock-empty activation delta from `47a8c007` to `bd95b9c0`, proving/encoding zero populated pages and zero Backup extents for the stock empty command while leaving owner integration and RunCompute `+0x83e` unreachable. Patch 0027 is the E102 compile-only serializer delta from `bd95b9c0` to `897d3ae4`, serializing the exact inactive stock-empty `0x2c0` SKU stream from runtime-owned inputs without allocating GPU memory or wiring RunCompute `+0x760`. Patch 0028 is the E104 compile-only SKU-backing delta from `897d3ae4` to `d54dfc78`, owning one exact zero-filled `0x30000` range-8 backing with `0xf0 × 0x300` geometry while exposing no selected FWVA and keeping the serializer disconnected. Patch 0029 is the E106 compile-only SKU-retirement delta from `d54dfc78` to `69af0148`, modeling exact next-slot scanning with a conservative `JobFence` guard while leaving backing writes and RunCompute publication unreachable. Patch 0030 is the E107 compile-only prepared-slot delta from `69af0148` to `f3464e7e`, copying exact stock-empty bytes into a caller-proven retired 0x300 slot and returning only an unreachable prepared token while leaving RunCompute `+0x760` unchanged. Patch 0031 is the E108 compile-only owner-graph delta from `f3464e7e` to `ea707e8e`, grouping the exact prerequisite backing owners under one unreachable construction boundary without exposing any FWVA or changing a RunCompute producer. Patch 0032 is the E109 compile-only unpublished-assets delta from `ea707e8e` to `a783f835`, materializing one private coherent address/HardwareBuffer token with rollback/completion ownership but no RunCompute consumer. Patch 0033 is the E110 compile-only guard-integration delta from `a783f835` to `63bb6ffe`, binding exact event/SKU rotation guards to an already-armed fence before the E109 materializer while keeping the wrapper unreachable. Patch 0034 is the E111 compile-only field-stage delta from `63bb6ffe` to `139b745d`, staging exact stock-empty command-facing pointers/values without providing any `RunCompute` writer or submission call site. Patch 0035 is the E113 compile-only two-phase delta from `139b745d` to `b21300a1`, separating slot/address reservation from command-FWVA-aware SKU serialization while keeping all unresolved inputs explicit and all live command producers unchanged. Patch 0036 is the E115 compile-only CL command-resource delta from `b21300a1` to `059d3470`, owning the exact `0x1f400` option-3 range-5 backing inside the unreachable graph and deriving its FWVA for dormant SKU finalization. Patch 0037 is the E117 compile-only channel-state-backing delta from `059d3470` to `252ded3b`, pinning the exact 0x24c0/0x8000/three-slot range-8 geometry and an unreachable backing block while keeping selected-slot FWVA and reset ownership gated. Patch 0038 is the E120 compile-only selected-state delta from `252ded3b` to `98d829e0`, reconstructing one already-selected 0x24c0 reset/priority image from exact J615 inputs while keeping the prepared token private and all SKU/RunCompute publication absent. Patch 0039 is the E121 compile-only selected-state gpu-buffer delta from `98d829e0` to `563b93f4`, deriving QueueInfo `+0x18` as selected-state address + `0xb0` and removing that one external private input without changing live WorkQueue ownership. E122 is a static exact-target channel-memory backing closure with no Linux source delta. Patch 0040 is the E123 compile-only delta from `563b93f4` to `14954ffd`, owning independent exact J615 `0x8000` range-7/range-8 channel-memory backing blocks while exposing no selected element FWVA and changing no live producer. Patch 0041 is the E124 compile-only delta from `14954ffd` to `1d9c455c`, modeling only the exact normal-J615 uncached selected-element reset header and returning no FWVA. Patch 0042 is the E125 compile-only semantic-correction/owner delta from `1d9c455c` to `adfbfca2`, proving QueueInfo `+0x10` is timestamp-queue state, owning one exact range-7 timestamp block privately, and leaving the live WorkQueue notifier path unchanged. Patch 0043 is the E126 compile-only scheduler-state delta from `adfbfca2` to `293ca633`, owning one exact 0x4000 special-range-8 scheduler backing and modeling only a private selected-state host reset while leaving the live generic GpuContext path untouched. Patch 0044 is the E127 compile-only channel-owner integration delta from `293ca633` to `8f6bd394`, grouping the exact channel backings under the dormant stock-empty graph and deriving `channel_state_fwva` internally without adding any live consumer. Patch 0045 is the E128 compile-only fixed-input delta from `8f6bd394` to `ccc976c5`, replacing arbitrary first-CL `evctl_index` and J615 accelerator bit-39 inputs with exact target constants while keeping the serializer and all live producers gated. Patch 0046 is the E129 compile-only GART-soft-fault delta from `ccc976c5` to `65451e3d`, removing the arbitrary target finalize boolean and supplying exact normal-J615 `false` while leaving the generic serializer parameterized. Patch 0047 is the E130 compile-only Compute-statistics-owner delta from `65451e3d` to `635fca8e`, replacing arbitrary target `firmware_state_fwva` with the typed manager-global `G15StatsComp` pointer while leaving the generic serializer parameterized. Patch 0048 is the E131 compile-only command-source delta from `635fca8e` to `251a60a0`, deleting the parallel scalar finalize-input bag and deriving exact command-local SKU values from the typed RunCompute image while keeping runtime publication unreachable. Patch 0049 is the E133 semantic-correction delta from `251a60a0` to `f09bed89`, correcting pre-micro `+0x740/+0x748/+0x750` from queue-state names to their exact raw Compute `+0xc0/+0xd8` sources while leaving the already-zero stock-empty bytes unchanged. Patch 0050 is the E134 compile-only fence-transaction delta from `f09bed89` to `a244a846`, arming the dormant two-phase event/SKU transaction before reservation with RAII pending-count rollback while keeping the live compute path and all RunCompute bytes unchanged. Runtime G15 submission remains fail-closed.

## Explicitly not enabled

- no production G15 render path claim
- no general command-buffer submission
- no RunVertex/RunFragment/RunCompute or shader payload enabled by the registration-only diagnostic
- no direct experimental PMGR writes
- no upstream AsahiLinux push implied by this public checkpoint

The project remains a staged clean-room bring-up with one-shot live gates and Golden-kernel recovery.

E147 corrects the remaining firmware-resource ownership placement before any dormant channel assembly can become reachable. Exact 23J220 places `_AGFIChannelState`, `_AGFITimeStampQueue`, cached/uncached channel memory and `_AGFISchedulerState` in five accelerator-owned `AGXFirmware` resource stacks with eager first backings and independent dynamic global indices. Selection uses common two-level lowest-free bitmaps, grows the global index space up to `0x800`, increments the selected backing use count and clears availability; release performs the inverse before any shrink. Scheduler/timestamp resources can be consumed before the first CL channel and TA/3D/CL channels share the three channel stacks, so none of the five indices may be hard-wired to zero or coupled to each other, event-control state or UMAPool identity. The E145/E146 zero-caller bundle stays safe but its E127-style per-channel local backing ownership is not liveable; the next integration layer must introduce device-global stack owners plus command-queue/channel selection leases. No Linux source delta is made in E147, the clean Linux checkpoint remains `b06c8650159b`, and live RunCompute remains fail-closed.

E148 implements the E147 selector/lifetime layer host-side without making any resource-stack GPU address reachable. Linux commit `1308998922f4` places five independently locked logical firmware resource stacks at G15 `GpuManager` lifetime, with the exact `0x800` global ceiling, eager first logical backing, two-level lowest-free bitmap selection, per-backing live-use counts, growth and inverse release. Real Queue construction retains timestamp+scheduler global-index leases for command-queue lifetime; the zero-caller unpublished Compute-channel assembler retains separate channel-state/cached/uncached leases. RAII rolls partial construction back automatically. Crucially, E127 local backing proof slots are not equated with these global selectors and E148 allocates no resource-stack GPU backing or selected FWVA. `queue/compute.rs`, `fw/compute.rs` and `workqueue.rs` are unchanged. The clean Linux checkpoint is `1308998922f4`, tree `8c1d9c20f68a`, published as patch `0060`; live RunCompute remains fail-closed. The next boundary is exact normal-J615 backing release/shrink policy followed by device-global mapped backing ownership and global-index-to-FWVA resolution.

E149 closes the target firmware-resource backing reclamation policy that E148 deliberately left conservative. Exact 23J220 initializes all five target stacks with `AGXFWPoolShrinkMode=0`; `allocateNewBlock()` therefore seeds each allocation use-count at zero, normal selection increments it, and normal scheduler/timestamp/channel release paths pass `false` to `releaseAllocationBlockIfPossible()`. When the last selected element drops the count to zero, the template immediately removes the backing's complete global-index range from the availability bitmaps, frees the backing, and leaves a backing-array hole. Free elements in later still-present backings remain preferred until the global bitmap empties, at which point growth may recreate the lower hole. Linux checkpoint `368783dd61e2` corrects only the host bookkeeping to model these absent backing slots and mode-0 destruction/recreation. No mapped resource-stack backing or selected FWVA is created, Compute producers remain unchanged, and live RunCompute remains fail-closed.

E150 connects the exact global resource-stack slot/lifetime model to the real shared-bank1 mapping definitions without making it reachable. The definition-only mapped factory uses exact prior geometry and protection classes: timestamp range 7 (0x18, 0x4000), scheduler range 8 (0x40, 0x4000), channel state range 8 (0x24c0, 0x8000), uncached channel memory range 7 (0x2860, 0x8000), and cached channel memory range 8 (0x2860, 0x8000). Selected FWVA derivation is `backing_base + local_element * element_size`, and E149 mode-0 last-use destruction now owns the optional mapped backing lifetime as well. Linux checkpoint `f17014117d4a` adds the dormant mapped constructor and a dedicated semantic range-8 channel-state allocator, but `new_mapped_device_global()` and `mapped_fwva()` each remain zero-caller; `gpu.rs`, `queue/compute.rs`, `fw/compute.rs`, and `workqueue.rs` are unchanged. No new runtime mapping is created. The next gate is exact boot-time q22 mapping-publication ordering before the host-only GpuManager constructor can ever be replaced; live RunCompute remains fail-closed.

E151 closes that q22 ordering gate statically on exact 23J220. The `AGXArmFirmware` constructor proves firmware `+0x1288` starts at zero via `x20 = firmware + 0xae8` followed by a zero byte store at `x20 + 0x7a0`; all five accelerator-global resource stacks have already created their eager first backing in `allocFirmwareData()` while that gate is clear. `notifyNewMapping()` tests `+0x1288`, and successful `prepareFirmwareData()` sets it to one under the firmware lock before the exact `prepare -> initFirmwareData -> bootFirmware` sequence continues. Those bootstrap PTEs are not replayed into q22. Linux checkpoint `823fb161085e` adds an inactive/active q22 notifier policy and marks only E150's still-dormant mapped resource factory as `AfterActivation`: bootstrap map/unmap remains silent while later growth or teardown after activation is q22-visible. Existing mappings default to immediate notification, the activation method has no live caller, `new_mapped_device_global()`/`mapped_fwva()` remain definition-only, and `gpu.rs` is unchanged. No runtime resource mapping, module installation, or RunCompute is introduced.

E152 moves the exact mapped firmware-resource owners from definition-only into the G15 manager graph without opening the hardware runtime gate. `build_pre_rtkit()` already constructs InitData/q22 first; `make_mgr()` now uses the shared bank-1 root and q22 notifier to create the five E150 mapped first backings under E151 `AfterActivation` policy. The existing G15 manager validator requires q22 ring-empty after the complete graph, so bootstrap notification remains fail-closed. After `MSG_INIT`, `GpuManager::init()` waits for q21 `firmware_ready == 1` and only then activates q22; DRM registration follows `gpu.init()`, so no Queue resource lease can precede activation. Pre-activation backing teardown is silent, post-activation growth/teardown is q22-visible, and failed unmap publication preserves the PTE/VA reservation. The current E075 G15 probe `ENODEV` gate is unchanged, as are `queue/compute.rs`, `fw/compute.rs`, and `workqueue.rs`; no selected resource FWVA reaches RunCompute. Linux checkpoint `d9f5cf4cddeb`.

E153 bridges the selected-state byte images onto the real E147-E152 mapped global firmware-resource leases and deletes E127's duplicate local timestamp/scheduler/channel backing layer. Exact 23J220 userspace proves an ordinary command queue starts from a fresh descriptor with `_openGLQueue=0`; IOGPU forwards that byte to `IOGPUDeviceNewCommandQueueArgs +0x404`, kernel `IOGPUCommandQueue::init()` stores the resulting false value at queue `+0x439`, and `AGXCommandQueue::init()` calls `AGXTimeStampQueue::setUpdateMode(false)`. The selected timestamp state therefore has `+0x10=0` exactly. Linux checkpoint `4a72f743dd25` initializes the real timestamp/scheduler selections once during Queue construction and initializes the real channel-state/cached/uncached selections in the still-zero-caller lazy CL-channel constructor. Five local backing owners, five local slot inputs and the per-command `timestamp_update_mode_2` input are removed. Only `channel_4c_value`, `effective_priority`, and `queue_qos` remain as dormant channel-construction scalars. Compute writer files remain unchanged, E075 remains closed, and live RunCompute remains fail-closed.

E154 closes the final E127-era scalar input boundary for the still-unpublished first normal J615 CL/Compute channel. Exact 23J220 kernel symbols identify `IOGPUDevice +0x60` as `proc_pid(get_bsdtask_info(task))`; that PID is copied through `IOGPUCommandQueue +0x490` and `AGXChannel +0x4c` into selected QueueInfo `+0x50`. Exact `IOGPUCommandQueue::updatePriority()` checks the `"foreground"` privilege and the non-foreground branch selects constructor field `+0x448 = 2`; exact AGX propagation keeps integer argument 2 and `chooseCLWorkQueue()` supplies literal QoS 2 when effective priority is not 1. Linux checkpoint `1e74793c7d14` captures the opening process PID at DRM File lifetime, deletes `G15ComputeChannelInitInputs`, and renames the zero-caller helper to the explicit non-foreground profile. Linux does not invent a foreground-entitlement mapping. Compute writer files remain unchanged, E075 remains closed, and live RunCompute remains fail-closed.

E155 closes the exact CL WorkQueue/channel lifetime before the dormant channel assembler can ever become reachable. Exact 23J220 stores the lazily created CL channel at `AGXCLWorkQueue +0x1e8` and stores the WorkQueue reverse pointer at channel `+0x1d8`. `AGXCLWorkQueue::finalize()` clears that reverse pointer under the channel lock before chaining to `IOGPUWorkQueue::finalize()`, while `AGXCLWorkQueue::free()` releases/clears the owned channel before inherited WorkQueue teardown; lazy-construction failure uses the same channel-first unwind. Linux checkpoint `440e026d0d9c` therefore adds an empty concrete Compute-channel slot plus an opaque clone owned by the WorkQueue itself, not merely the parent SubQueue. This is necessary because WorkQueue Jobs and the event manager retain independent WorkQueue Arcs. The slot has no setter, the E154 assembler remains definition-only, Compute writer files are unchanged, E075 remains closed, and live RunCompute remains fail-closed.

E156 adds only the definition-only lazy first-CL ensure transaction on top of E155. Exact `chooseCLWorkQueue()` keeps a newly allocated CL WorkQueue local through WorkQueue init, channel creation/init and priority application, then increments `+0x890` and publishes the WorkQueue into the command-queue `+0x5f0` array; failure releases channel then WorkQueue without publication. Linux checkpoint `446383749394` locks E155's empty WorkQueue-owned channel slot, constructs the complete E154 channel locally, and stores `Some(channel)` only on success. The slot has no other mutator or reverse lock path, and the ensure helper itself has zero callers. Compute writers, WorkQueue implementation, E075 gate and live RunCompute remain unchanged.

# AirM3Gpu

Experimental, clean-room bring-up work for the Apple M3 / T8122 G15 GPU on Asahi Linux.

This repository is the **public, reproducible checkpoint** for the work. It intentionally does not contain Apple firmware, kernelcaches, extracted proprietary binaries, Ghidra databases, boot images, built kernel modules, or raw machine-local captures. Those are kept outside this repository; only derived facts, original research notes/scripts, hashes, and source patches are published here.

## Current research state — 2026-08-28

Target hardware: MacBook Air M3 (J615 / T8122), GPU identified at runtime as G15G C0.

The bring-up has advanced well beyond the earlier pre-`MSG_INIT` checkpoint. The isolated candidate now reaches native G15 `MSG_INIT`, post-init bootstrap, persistent RTKit/GpuManager lifetime, DRM registration, and `/dev/dri/renderD128`. Safe host-side GET_PARAMS, VM/GEM lifecycle, unbound VM mappings, passive queue lifecycle, q22 mapping notification, and shared-bank1/range-7 page-table handling are live-tested.

The bounded registration path now reaches and returns from the real G15 scheduler. E056 identified a mis-modeled 0x54-byte DPE leakage-update image at HwDataA `+0x4188`; restoring its exact J615/C0 values removes the earlier cold-power/DPE retrigger loop. E057 then produces scheduler `0x112` snapshots and an accepted `0x111` RunWorkQueue record.

E058-E060 close the remaining registration prerequisites without executing GPU work. A valid stamp number alone is insufficient; a firmware Barrier/type-4 record is required to bind the RunWorkQueue stamp slot to QueueInfo `gpu_buf`. With one already-satisfied Barrier at ring entry 0, the pipe retires to `(Read, CFI, Write, Shadow) = (1,1,1,1)` and callback exit `0x101` appears. E060 also corrects G15 `ReleaseResource` enum numbering to Apple's opcode `0x11`; native teardown then succeeds and firmware returns from software state 1 to state 0 without a crash.

No RunVertex, RunFragment, or RunCompute command and no shader stream is exposed by this proof. E061 now proves that Apple's normal RunCompute/type-3 path is inherently hardware-facing: RTKit copies command `+0x760` into the per-stamp execution record, while Apple's producer always builds a real G15 CDM RegisterArray and SKU stream. There is no justified parser-only RunCompute shortcut. The current boundary is closing the minimum real Compute register/SKU/UMA/completion contract before the first hardware-facing command. E062 closes the SKU packet grammar itself: the no-feature stream is exactly `0x2c0` bytes after alignment, Compute WFI is the single dword `1`, timestamp records are fixed 0x3c-byte opcode-3 packets, and the remaining J615 dynamic register IDs are `0x101d8` and `0x107a0`. E063 additionally proves that RegisterArray `0x1a420` receives the raw CDM control-stream pointer and reconstructs Apple's Gen4 patch/reset framing (`0x60000160` / `0x60000960` plus 16-byte `0x20000000` pointer records). E064 classifies those exact tokens: `0x60000160/0x60000960` are CDM Barriers, `0x20000000` is Stream Link, and Apple G15 emits `0x40000000` as Stream Terminate. The first plausible no-launch payload is therefore a terminate-only CDM root; the unresolved boundary is proving that Apple's normal zero-dispatch Compute container accepts that form with valid surrounding state.

E065-E162 close the next exact-target layers. The stock no-dispatch Compute container was captured and then validated against the matching macOS 14.8.3 / 23J220 G15G driver: the ordinary RegisterArray is exactly 20 entries, the empty descriptor reaches normal `submitBuffer()`, G15 RunCompute `+0x760` is the SKU execution stream rather than the inherited Linux microsequence, and the inactive stock-empty stream is exactly `0x2c0`. E071 corrects the prepared UMA byte at RunCompute `+0x846` to `1`; E072 proves empty-path `+0x847/+0x84f` are zero while `+0x83e` and `+0x857` are live Page-Pool-State/HWMetrics pointers; E073 closes the 64-ID managed context lifecycle and wires the per-ID generation byte to RunCompute `+0x85f`. E074-E076 prove the range-8 parent/leaf ownership and split bank-1 VA allocation without instantiating the FList object. E077 then closes the exact four-resource `AGXUMAFList::init()` contract: 0x70 Page-Pool State in range 8, 8-byte FW-Uncached State in a distinct range-7 class, and exact symbolic Page/Backup List size formulas while deliberately leaving their override-sensitive numeric sizes conditional. E078 models that second range-7 class as leaf `0x00c000000000044b`, adds the exact 8-byte ABI and a hard-wired allocator constructor, while preserving the existing PM/q22 leaf `0x00c0000000000447`. E079 then reconstructs the 0x100-entry HardwareBuffer-ID state machine itself: sticky owner IDs, per-ID references, the zero-reference bitmap, Apple's initial 0→255 allocation order, bitmap fallback/steal behavior, and the 0→1 / 1→0 prepare/complete transitions. E080 wraps that exact state in the driver's established kernel mutex/Arc lifetime and adds an FList-side sticky owner while deliberately creating no runtime instance. E081 then adds a side-effect-free FList resource plan: exact checked symbolic Page/Backup List formulas, compile-time 0x70/8-byte fixed-object sizes, and the synchronized sticky HardwareBuffer owner, but no `GpuObject`, `GpuArray`, allocator handle or command pointer. E082 closes the remaining list mapping class: the exact FList caller option is `0x20000000003` (correcting an older transcription that omitted two zeroes), range-5 outer option `0x20500000007` reduces to compact UAT `0x300`, and Linux gives that cached PTE shape a dedicated FList semantic constant without allocating anything. E083 then proves the J615 override fields remain zero from zero-filled object allocation through configure/start, making the G15G getter defaults exact: M=2 GiB and B=4 MiB. With the exact 16-KiB page, the FList list sizes are 4 MiB / 0x80000 entries and 32 KiB. E084 then cross-checks current public m1n1 AGX structures as a semantic dictionary only: it reinforces Start/Finalize-class SKU, dual-stamp/event-control, page-list/manager, context-vs-VM, multi-counter/timestamp, and queue-state concepts without importing any old offset or opcode. E085 closes the q22 prerequisite for the future range-8 Page-Pool-State mapping: ordinary range 7 remains map/unmap `1/0`, while Apple's special 64-MiB range 8 uses exact `3/2`. E086 then adds the four-object FList persistent backing owner as unreachable compile-only code, stopping at the exact constructor seed and exposing no Page-Pool-State command pointer. E087 closes the lower-level residency lifetime: first HardwareBuffer activation prepares/commits the mappings, later complete drops preparation references without releasing PTEs, and PTE teardown is a separate FList lifetime. E088 then closes the normal success-path stamp/retirement chain and proves RunCompute `+0x14` is the rotating command-buffer event-control FWVA, distinct from the generic descriptor submit event that gates WorkQueue pruning. E089 removes the inherited wrong-lifetime queue-wide notifier from that G15 field and leaves the event-control FWVA zero/fail-closed until the exact 36-state owner exists. E090 closes the recovery side: destructive restart force-completes stamps into the ordinary scheduler/prune/descriptor-complete path, then separately drains cached context state, pending UMA grow/request references, sticky FList HardwareBuffer ownership, page-pool state and channel state; preserve/restart states remain distinct and are not given guessed enum names. E091 then models the exact rotating event-control ownership geometry—36 opaque 0xc0-byte states, contiguous 0x1b00 total, increment-before-select modulo 36—without implementing `GpuStruct`, choosing a mapping class, allocating GPU memory or publishing RunCompute `+0x14`. E092 closes that mapping ambiguity on exact 23J220: the command buffer uses one contiguous 0x90-byte stamp backing in normal range 7 (`0x700000007`, leaf `0x...0447`) and one contiguous 0x1b00-byte event-control backing in range 8 (`0x20800000007`, leaf `0x...0443`); construction zeroes the pools and links each control `+0x00` to its matching stamp FWVA, while the rotation-time `+0x10` config value remains deliberately unresolved. E093 promotes only that proven construction boundary into unreachable compile-only ownership: exact-base shared-data allocations for the two pools, q22 ownership, and per-control `+0x00` stamp linkage are modeled, but the owner has zero call sites and RunCompute `+0x14` remains zero. E094 closes the selected-state numeric seed on normal J615: accelerator `+0x2428` remains zero, so event-control `+0x10` is exactly the 80-record G15 fallback; Linux can reproduce the post-finish reset/seed image while deliberately exposing no selected FWVA. E095 then proves slot rotation is guarded by host-event retirement and corrects event-control `+0x08` to the unmasked shared state sequence, not a stamp index. E096 models that reuse barrier with the existing Linux per-submission `JobFence`: occupied/in-flight slots remain unavailable until their pending command count reaches zero. E097 then closes the separate HWMetrics pointer producer: exact 23J220 owns one zeroed 0x4000-byte range-7 page, advances 0x40-byte records modulo that page, maps it with option `0x0001008700000007`, reduces to compact UAT `0x30b`, and emits exact bank-1 leaf `0x00e000000000040b`, bit-identical to Linux `PROT_GPU_FW_SHARED_RW`. E098 adds only an unreachable page-base HWMetrics owner and hard semantic aperture/protection pairing; RunCompute `+0x857` remains zero. E099 then models the exact post-`populatePagePool()` FList firmware-state image behind the first HardwareBuffer epoch, validating dynamic page/list counts and publishing only owned Page/Backup/FW-Uncached FWVAs into the unreachable 0x70 state. E100 closes the stock-empty first-activation contents specifically: the exact type-5 UMA accounting oracle stays zero around a successful empty Compute command, proving no nonzero pool-growth path; the Page/Backup lists therefore remain zero-filled, page count/cursors and Backup extent count are zero, while mapped resources, capacity `0x80000`, and the real HardwareBuffer ID remain real. Linux models that exact empty image in an unreachable helper and still exposes no `+0x83e` pointer. E101 then re-audits the exact 23J220 inactive stock-empty SKU payload source-by-source, correcting the setup-payload `+0x3c/+0x40` split and timestamp UUID field while closing the type-0xc backpointer/bookkeeping and J615 channel-region geometry. E102 implements that map as an unreachable byte-exact `0x2c0` serializer whose inputs are all runtime-owned addresses/state; it owns no GPU memory, has zero call sites, and does not change RunCompute `+0x760`. E103 then closes the encoder backing itself: normal J615 has exactly `0xf0` SKU slots at `0x300` bytes, one page-rounded `0x30000` special-range-8 backing, and a separate `0x3c00` host event array; the range-8 mapping is the already-live-proven `0x...0443` class and is retained for encoder lifetime. E104 models only that GPU backing as an unreachable zero-filled page-base owner with no selected-slot/FWVA accessor and no link to the E102 serializer. E105 then closes exact SKU slot reuse: Apple scans from the next slot modulo `0xf0`, requires the per-slot command event to test complete, copies the new command event into the selected host guard, and returns GPU-base + `slot*0x300` from `finishEncode()` as RunCompute `+0x760`. E106 models that reuse rule conservatively with the existing whole-submission `JobFence`: it can delay reuse but cannot permit premature overwrite, and remains definition-only. E107 adds that copy boundary without publishing it: after an E106-retired slot is supplied, Linux clears the complete `0x300` slot, copies exactly the E102 `0x2c0` stream, preserves the final `0x40` zero slack, and returns only a definition-only prepared token with slot/FWVA/size. E108 then groups the exact event-control, HWMetrics, FList/HardwareBuffer and SKU backing owners under one unreachable construction graph sharing the same bank-1/q22 lifetime while preserving their distinct activation/retirement epochs. The graph exposes no FWVA, selects no slot and has zero call sites. E109 now materializes that private, non-Copy assets token from caller-retired event/SKU slots: event-control FWVA, exact `0x2c0` SKU FWVA/size, initialized Page-Pool-State FWVA, current HWMetrics FWVA and HardwareBuffer ID are kept together with rollback/completion ownership, but the token has no RunCompute conversion and zero external call sites. E110 integrates those two retirement guards into the definition-only path: an already-armed `JobFence` must bind the exact next event-control state and the first reusable SKU slot before E109 can materialize its token; busy event-control selection does not advance past the blocked state, and fresh guard bindings roll back on failure. E111 converts those guarded private assets into a host-only RunCompute field stage for exact stock-empty `+0x14`, `+0x760/+0x768`, `+0x83e`, `+0x846/+0x847/+0x84f`, and `+0x857` values while retaining the non-Copy HardwareBuffer lifetime. It has no firmware-command writer. The current clean Linux checkpoint is `139b745dbf00`, published as patch `0034`. E112 performs that audit and blocks a direct writer: exact Apple ordering requires a two-phase command/SKU transaction, current Linux arms the fence too late for E110, several SKU runtime sources are not bridged, and active pre-SKU/JobParameters2 fields remain unclosed. E112 makes no Linux source change. E113 implements only that two-phase dormant boundary: phase 1 reserves exact rotating slots and command-independent assets without SKU bytes; phase 2 requires the future command FWVA plus all still-unclosed runtime inputs before serializing/writing the reserved slot. It still has no RunCompute writer. E114 then closes the exact J615 CL-channel command-resource geometry used by that SKU path: two `0xf400` regions plus one `0x800` MGPU span and the fixed low term produce one persistent `0x1f400` option-3 range-5 resource; the same oracle also pins Compute `JobMeta.evctl_index` to channel `+0x38` and `flush_stamps` to `(descriptor+0x48 != 0)`. E115 adds only an unreachable owner for that range-5 backing and makes dormant SKU finalization derive the CL command-region base from it instead of accepting another external pointer. E116 then closes the separate firmware `_AGFIChannelState` backing used by the SKU channel-state pointer: exact states are 0x24c0 bytes, firmware allocates 0x8000-byte special-range-8 blocks with three slots at 0/0x24c0/0x4980, and `AGXChannel +0x90` is the selected slot FWVA after reset. E117 models only that backing geometry as an unreachable special-range-8 owner with no selected FWVA accessor or QueueInfo initialization. E118 then closes the selected-state reset/priority mutation ABI and proves the inherited Linux priority table is not a mechanically valid direct G15 priority table; the ordinary CL constructor/priority arguments remain explicit. E119 closes those constructor sources: the first CL channel `evctl_index` is its CL-workqueue index (0 for the first channel), the second constructor integer is J615 `0x50`, and the priority tuple is derived from effective IOGPU priority/QoS with exact integer argument 2. E120 then reconstructs one already-selected 0x24c0-byte channel-state slot exactly at the proven reset/priority boundary while deliberately keeping resource-stack slot index separate from `evctl_index`; it returns only a private prepared-state token with no SKU or RunCompute conversion. E121 closes QueueInfo `+0x18`: Apple derives it from selected-state GPUVA `+0xb0`, exactly the end of the 0xb0 QueueInfo prefix, so Linux removes that address from the private external inputs without changing the live WorkQueue allocation model. E122 closes QueueInfo `+0x00/+0x08` backing provenance: normal J615 uses separate uncached range-7 and cached range-8 channel-memory stacks with identical `0x2860` elements, `0x8000` backing blocks and three slots, mapped as the already-proven `0x...0447` / `0x...0443` classes. E123 owns one block of each class in unreachable compile-only Linux code with no selected-element FWVA accessor or live-channel integration. E124 then closes the selected uncached-element reset header: normal J615 derives `channel+0x54 = 0x500`, and reset writes only u32 words `+0x00/+0x10/+0x20/+0x30/+0x40 = 0` plus `+0x50 = 0x500`; Linux models those six writes behind a private local-slot token with no FWVA. E125 corrects QueueInfo `+0x10`: the exact producer is the selected `AGXTimeStampQueue` state, backed by a 0x4000 normal-range-7 block of 0x2aa × 0x18-byte states, not the inherited queue-wide notifier list. Linux corrects only the private semantic input and adds an unreachable timestamp-state owner/reset model while leaving the live WorkQueue constructor unchanged. E126 closes QueueInfo `+0xa4`: exact command queues select one 0x40-byte `_AGFISchedulerState` from a 0x4000 special-range-8 backing with 0x100 slots, and the host reset initializes exactly the first 0x38 bytes while preserving the final eight. Linux adds only an unreachable semantic owner/reset and corrects the dormant source name; the live generic `GpuContext` path remains unchanged. E127 groups timestamp, scheduler, channel-state, cached/uncached channel-memory and CL command-resource ownership into the existing unreachable stock-empty graph. The five resource-stack indices remain independent, one private coherent QueueInfo pointer set can be reconstructed, and SKU finalization no longer accepts an external `channel_state_fwva`. E128 then closes two exact normal-J615 SKU inputs at that dormant boundary: the first CL channel uses `evctl_index=0`, and exact base/G15/G15G configuration proves accelerator packed-feature bit 39 (`+0x654` bit 7) is zero. E129 closes the last boolean ambiguity in the E101 setup packet: the ordinary IOGPU device path opens user-client type `0x00000005` with options zero, propagating zero through IOGPUDeviceUserClient/AGXShared/AGXSecureGart, so normal-J615 `AGXGart::isHWSoftFaultEnabled()` is false. Linux removes that target-specific finalize input while keeping the generic serializer parameterized. E130 then identifies the remaining `firmware_state_fwva` source as Apple's exact `0xe10` Compute-statistics allocation: descriptor index 3 pairs `AGXFirmware +0x228/+0x268`, base initialization zeroes all `0xe10` bytes, shared-data `+0x244` and the SKU encoder consume the same FW-visible object, and Linux already owns the exact `G15StatsComp` counterpart under manager-global RuntimePointers. Linux removes the raw finalize address and carries a typed `G15StatsComp` weak pointer only inside the dormant owner graph. E131 then closes the remaining command-local finalize scalars from the already-initialized RunCompute image itself: counter `+0x04`, managed context ID `+0x10`, JobMeta FW stamp/value `+0x7f0/+0x7f8`, UUID/event sequence `+0x808/+0x80c`, and user-timestamp presence `+0x828/+0x830`. The parallel target-specific finalize-input struct is deleted; the generic serializer remains parameterized. E132 recovers the exact 23J220 `AGXMetalG15G_C0` producer and proves the stock-empty JobParameters2 qwords at RunCompute `+0x774/+0x77c/+0x784/+0x78c/+0x7a4/+0x7ac/+0x7b4/+0x7bc` plus Compute `G15JobMeta.engine_state` are exactly zero. E133 then corrects the older pre-micro alias: RunCompute `+0x740/+0x748/+0x750` come from raw Compute `+0xc0/+0xd8`, not `AGXCommandQueue` state, and exact same-build stock-empty values are also zero. E134 closes the dormant fence-order prerequisite with a private RAII command arm carried through the two-phase prepare/finalize tokens, while leaving the ordinary `submit_compute()` path and every RunCompute byte unchanged. E135 then closes the persistent UMA placement topology: the 0x100-entry `UMAPool` HardwareBuffer-ID namespace is accelerator-global, normal CL channels retain one of two priority-class Compute pools from a four-slot shared-pool container, and the firmware-visible pool ID is globally creation-ordered rather than a Compute constant. This blocks a naïve per-Queue instantiation of the current all-in-one owner graph. E136 implements the required definition-only split: one device-global UMA state now owns the 0x100 HardwareBuffer-ID namespace plus global pool-ID sequence, one reusable shared Compute pool owner contains the FList, and the dormant channel/command owner no longer contains or constructs any FList. Two-phase UMA operations now require that shared pool explicitly; live `submit_compute()` remains unchanged. E137 then closes the shared-pool retain/replacement lifetime: container slots are weak pool references promoted by a nonzero-only atomic try-retain under the container lock; every channel has a separate direct pool reference; pool `+0x20` carries a second aggregate active-channel epoch retain; finalization pointer-checks and clears only its matching weak slot before cleanup releases the pool-owned container reference. This rules out a naïve strong `Option<Pool>` Linux container. E138 proves pool creation itself is lazy: command-queue and bare workqueue initialization create no TA/3D/CL channel, while `chooseCLWorkQueue()` and `allocate3DWorkQueue()` reach exact J615 `newCL/+0xb48`, `new3D/+0xb40` and `newTA/+0xb38` factories only when those paths are first required. Linux can therefore place one zero-seeded G15 UMA host state at device lifetime without consuming a pool ID or creating a UMAPool. E139 then closes the FList mapping scope: `AGXShared` owns one client task/IOGPUTask pair plus its own shared-pool container, both range-5 FList list mappings target that same client IOGPUTask, and the existing Linux per-`file::Vm` range-5 allocator is therefore the correct address-space source. The remaining shared-pool container must be client-VM scoped rather than per Queue or device-global. E140 places exactly that empty four-slot weak-identity state beside the per-VM range-5 allocators, with no strong pool owner, no Queue accessor, and no pool-ID consumption; exact `AGXShared::free()` independently confirms the container is released at client lifetime while container free never releases the weak slots. E141 adds the safe logical nonzero-only promotion/direct+active lifetime state machine for those identities, with exact Compute slot selection and identity-checked final clearing. E142 then proves the shared-container lock remains held across failed promotion, new UMAPool construction/initialization and slot publication, and couples the actual shared Compute pool owner to that transaction without giving the slot an independent logical retain. E143 retains the exact per-client shared-pool container from `file::Vm` through Linux Queue lifetime, matching the command-queue-to-AGXShared provenance, but still exposes no selection/create call and consumes no pool ID. E144 retains the remaining per-client range-5 allocator and shared bank1/q22 handles needed by a future lazy Compute-channel transaction, with no constructor call or Compute producer change. E145 then type-connects those retained inputs to the client weak-pool transaction, device-global UMA identity/FList construction and separate channel-local owner set behind one zero-caller unpublished bundle; the Linux-only sticky HardwareBuffer owner cookie now derives from the globally ordered pool identity. E146 closes the normal shared Compute pool selector itself: exact initial/runtime context types 3/1/2 all map to class 1, so the dormant assembly no longer accepts an arbitrary pool class. E147 then proves the five timestamp/scheduler/channel-state/channel-memory selectors are independent accelerator-global firmware resource stacks with eager backing and dynamic global indices rather than per-channel local slots. E148 models that common 0x800-index bitmap/use-count transaction once at GpuManager lifetime and retains RAII timestamp/scheduler leases in real Queue lifetime plus three independent channel-resource leases in the still-zero-caller unpublished Compute bundle; it allocates no firmware-stack GPU backing and deliberately keeps E127 local proof slots separate. E149 then closes the exact mode-0 backing lifetime: target backings seed their allocation use-count at zero and are removed, with their whole global-index range, when the final selected resource is released; lower backing holes remain unavailable until all still-present backing free slots are consumed and growth recreates the hole. E150 type-connects those dynamic global backing slots to the already-proven range-7/range-8 allocators and exact backing/element geometry, deriving private selected FWVAs while keeping both the mapped constructor and FWVA accessor at zero callers. E151 proves the exact q22 bootstrap asymmetry: all five eager first backings exist while firmware `+0x1288` is constructor-zero, successful `prepareFirmwareData()` sets that gate to one, and the first backings are never replayed into q22. Linux therefore gains a monotonic notification policy that suppresses only pre-activation resource maps while retaining post-activation unmap/growth notification semantics. E152 then integrates the mapped global resource stacks into the G15 manager graph and arms q22 only after q21 `firmware_ready`, before DRM registration; the existing pre-RTKit ring-empty validator becomes a fail-closed bootstrap-publication check. E153 closes the ordinary timestamp-mode producer as false, initializes the real selected timestamp/scheduler states at Queue lifetime and the three real selected channel resources at dormant CL-channel lifetime, and deletes the five E127 duplicate local backings plus their local slot inputs. E154 closes the remaining dormant channel scalar producers: QueueInfo `+0x50` is the owning process PID, exact non-foreground effective priority is 2, and the corresponding first-CL QoS argument is literal 2. Linux captures the per-client PID at File open and removes the last scalar input bag while keeping foreground policy separate and the unpublished helper zero-caller. E155 closes the lazy CL WorkQueue/channel lifetime: exact Apple stores the channel at WorkQueue `+0x1e8`, clears the channel `+0x1d8` backpointer during WorkQueue finalization, and releases the channel before inherited WorkQueue free. E156 adds the still-zero-caller first-use transaction: it serializes on that WorkQueue-owned slot, constructs the full channel locally, and publishes `Some(channel)` only after success, matching Apple's post-init WorkQueue publication/failure unwind. E157 then retires only the obsolete E075 early stop and one-shot live-proves the current E147-E152 mapped global-resource manager through RTKit `MSG_INIT`, q21 `firmware_ready`, and empty-ring q22 activation for more than two minutes of fault-free idle time, while deliberately withholding DRM registration. E158 advances one boundary farther: DRM registration is live-proven with `renderD128` present, while an immediate G15 `File::open()` `ENODEV` gate prevents any client, VM, Queue, QueueInfo, q22 runtime mapping, channel ensure, or submission from existing. E159 then permits discovery-only G15 File lifetime: render-node open, `GET_PARAMS`, and `GET_TIME` are live-proven, while VM/GEM/map/Queue/destroy/submit ioctls are rejected before their first mutation. An all-zero `VM_CREATE` returned `ENODEV` before validation, and repeated discovery remained fault-free beyond two minutes with no q22 runtime publication. E160 advances private VM create/destroy only: the current per-client bank-0 root, inert range-5 allocators and empty UMA weak-slot container are live-created without `Uat::bind()`, while GEM and Queue creation remain gated. A valid VM lifecycle plus 16 repeated cycles passed for more than three minutes with no bind-slot marker, q22 runtime publication, or GPU/firmware/kernel fault. E161 advances host GEM lifetime only: normal and VM-private GEM handles plus DRM mmap offsets/CPU mappings are live-proven while VM_BIND and Queue creation remain gated. Public/private GEM CPU readback and 16 repeated lifecycle cycles passed with no user UAT bind, q22 runtime publication, or fault. E156 channel ensure remains unreachable and no selected resource FWVA reaches a RunCompute writer. E162 then permits normal `VM_BIND`/unbind only against the private, still-unbound G15 bank-0 VM root. A one-shot live probe mapped a 16 KiB RW GEM at `0x4000`, proved Queue creation still returned `ENODEV`, unbound and destroyed the VM, and repeated the cycle 16 times plus later probes with no firmware-visible bind/slot marker, q22 runtime publication, or fault. The current clean Linux checkpoint is `87bcd27a516d`, published as patch `0074`. Live RunCompute remains fail-closed.

See [Current State](docs/CURRENT-STATE.md), [G15 Queue Registration Lifecycle](research/g15/G15-QUEUE-REGISTRATION-LIFECYCLE.md), [G15 Pipe Submission Boundary](research/g15/G15-PIPE-SUBMISSION-BOUNDARY.md), [G15 Pipe Callback Gate](research/g15/G15-PIPE-CALLBACK-GATE.md), [Workflow](docs/WORKFLOW.md), [Patch Bases](docs/PATCHES.md), and [Recovery](docs/RECOVERY.md).

## Repository layout

- `docs/` — checkpoint, workflow, recovery, and patch-base documentation.
- `patches/linux/` — Linux/Asahi checkpoint patch against the recorded kernel base.
- `patches/mesa/` — Mesa/Honeykrisp patch series against the recorded Mesa base.
- `research/g15/` — curated original G15 reverse-engineering notes and clean-room generators.
- `manifests/` — hashes and component revisions for the current tested checkpoint.

## Status

This is bring-up/research code, not a production driver. Every live gate is intentionally staged and fail-closed. Do not treat the patchset as general M3 GPU enablement yet.
